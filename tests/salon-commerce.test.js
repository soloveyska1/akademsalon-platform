const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const M=require('../assets/js/salon-commerce.js');
test('24-hour express doubles the work only, and faster requests have no invented total',()=>{let q=M.quote({product:'course',speed:'express24',addons:['defense','norm']},14000);assert.equal(q.work,28000);assert.equal(q.extras,9500);assert.equal(q.total,37500);assert.equal(q.saving,1500);q=M.quote({product:'course',speed:'expressfast',addons:['defense']},14000);assert.equal(q.total,null);assert.equal(q.work,null);assert.equal(q.extras,6000);assert.equal(q.needsQuote,true)});
test('candidate work cannot inherit express when switching products',()=>{const s=M.normalize({product:'kandidat',speed:'express24'});assert.equal(s.speed,'standard');assert.equal(M.quote(s,200000).total,200000)});
test('known protection bundle replaces both lines and does not double charge',()=>{const q=M.quote({product:'diplom',addons:['norm','defense','norm','tutor']},40000);assert.equal(q.lines.length,2);assert.equal(q.lines[0].service,'defensepack');assert.equal(q.extras,12500);assert.equal(q.total,52500);assert.equal(q.saving,1500)});
test('VIP includes selectable extras and requires an individual final quote',()=>{const s={product:'course',package:'vip',addons:['defense','norm','tutor']};assert.deepEqual(M.lines(s),[]);const q=M.quote(s,14000);assert.equal(q.total,null);assert.equal(q.extras,0);assert.equal(q.needsQuote,true);assert.match(M.summary(s),/единая|единую/);assert.match(M.summary(s),/Количество итераций/)});
test('persisted composition is finite, unique and excludes client content',()=>{const s=M.normalize({product:'course',speed:'express24',package:'vip',scope:'part',addons:['norm','unknown','norm'],topic:'private',contact:'private',files:['secret'],consent:true});assert.deepEqual(Object.keys(s).sort(),['addons','package','product','scope','speed','version']);assert.deepEqual(s.addons,['norm']);assert.equal(JSON.stringify(s).includes('private'),false)});
test('unknown partial prices remain unknown even when paid extras are chosen',()=>{const q=M.quote({product:'custom',addons:['defense']},null);assert.equal(q.total,null);assert.equal(q.lowerBound,6000);assert.equal(q.needsQuote,true)});
test('homepage removes large distractions and form loads the same composition contract',()=>{const home=fs.readFileSync('index.html','utf8'),form=fs.readFileSync('configurator.html','utf8');assert.match(home,/data-home-workbench/);assert.match(home,/data-home-faq/);assert.doesNotMatch(home,/data-sample-studio|data-journey-preview|ПЛАНЫ НА СЕМЕСТР/);assert.match(home,/samples.html/);assert.match(form,/salon-commerce.js/);assert.ok(form.indexOf('salon-commerce.js')<form.indexOf('salon-order.js'))});

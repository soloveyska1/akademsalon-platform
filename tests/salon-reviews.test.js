const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),{execFileSync}=require('node:child_process'),path=require('node:path');const root=process.env.SALON_PUBLIC_ROOT||path.resolve(__dirname,'..');
test('gallery retains only exactly the previously published source images and captions',()=>{const old=execFileSync('git',['show','045181e4:reviews.html'],{encoding:'utf8'}),now=fs.readFileSync(path.join(root,'reviews.html'),'utf8');const images=s=>[...s.matchAll(/<img[^>]+src="(assets\/img\/reviews\/thumbs\/[^\"]+)"[^>]+alt="([^\"]+)"/g)].map(m=>[m[1],m[2]]);assert.deepEqual(images(now),images(old));assert.equal(images(now).length,47);for(const [src] of images(now))assert.ok(fs.existsSync(path.join(root,src)));assert.match(now,/<dialog[^>]+aria-labelledby="rv-title"/);assert.doesNotMatch(now,/AggregateRating|ratingValue/);});

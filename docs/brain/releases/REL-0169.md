# REL-0169 — ощутимая выгода первого заказа

- Date: 24 August 2026, Europe/Moscow.
- Canonical Salon source:
  `51f3556210d2821df17829d4b7776ad2f1dee948`.
- Verified product implementation:
  `bd2334f2459f106ebea15241359101164ab5c7bd`.
- Canonical Kladovaya source:
  `58f4c116344608fa4224f8f086346ce3c43a421e`.
- Product workstream: `WS-838673b2da9d47caa2d07a87b8a11a7e`.
- Release workstream: `WS-437f36bc78344556989810b3aba47daf`.
- Salon production path: `release169-51f3556`.
- Kladovaya production path:
  `20260824T1214Z-promo-mobile-58f4c11`.
- Published: `2026-08-24T12:18:36Z`.
- Verdict: **GO**, P0=0/P1=0 after three independent reviews, two-vantage
  smoke, production Chromium and executed backend plus static
  rollback/forward drills.

## Outcome

`ПЕРВЫЙЛИСТ` now gives a material 12% on one first order from 2,500 RUB, capped
at 5,000 RUB. The exact examples shown to the client are 300, 600, 1,200 and
2,400 RUB at 2,500, 5,000, 10,000 and 20,000 RUB; from about 42,000 RUB the cap
is 5,000 RUB. Qualified return-to-draft is 10% from 5,000 RUB, capped at 2,500
RUB and valid for 72 hours.

Known old users remain suppressed. A clean anonymous sheet is provisional;
order creation atomically repeats account, canonical contact, order-history
and exact family/code/order checks. Existing v1 seen keys keep prior visitors
suppressed. Authenticated Salon owner and Kladovaya's explicit bookmark remain
labelled non-redeemable previews.

Promo and Salon+ are best-of, not stacked. Promo/subscription plus spent bonus
points is capped at 25% of the agreed price. This bounds exposure but does not
prove a positive margin without authoritative fulfilment-cost and commission
data; profit uplift is not claimed before measurement.

## Release proof

- Salon 603/603, backend campaign 19/19, Kladovaya 277/277 plus lint and Brain
  39/39 passed on the exact closure.
- The backend completed two-phase PID-attested activation, executed safe
  rollback to campaign-off/coherent-v2 and explicit forward enable. Final
  source is installed/on, SQLite is `ok` and open-order anomalies are 0/0.
- Salon immutable tree: 359 files / 26,181,248 bytes, manifest
  `d1dd6f13...9460b`; Kladovaya: 559 files / 50,053,327 bytes, manifest
  `c95f656c...c665`.
- GET-only smoke passed 14/14 from the operator network and 14/14 on the VPS.
- Production Chromium proved Salon clean-new and returning suppression,
  production owner-preview layouts, Kladovaya owner preview at 360/390,
  body/focus/overflow contracts and zero console errors.
- A production-only P2 overlap in Kladovaya's owner label was fixed before the
  final immutable release; measured preview/close overlap is now zero.

Exact hashes, operator-observation caveats, backup paths and browser evidence
are in `E-1031`.

## Live result and rollback

Salon `current`/`dist` resolve to `release169-51f3556`; `previous` resolves to
`release168-afe4755`. Static pointer backup is
`/root/site-backups/release169-promo-value-20260824T1203Z`.

Backend rollback uses
`/root/salon_bot/backups/first-order-promo-economics-20260824T120008432055Z`.
It keeps the aggregate v2 guard and promised rows while turning new campaign
activity off. Never toggle the campaign separately while a staged transition
sentinel exists; finish finalize or rollback first.

Kladovaya `current` resolves to
`20260824T1214Z-promo-mobile-58f4c11`; its final pointer backup is
`/root/site-backups/20260824T1214Z-promo-mobile-58f4c11`.

No live form, order, contact, file, payment, message, analytics event, claim or
retention grant was created during release proof.

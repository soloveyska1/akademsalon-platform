# REL-0166 — приватный checkpoint конфигуратора

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `a9e038ed62c126e3d54290cfda547c50bd290772`.
- Verified product result: `eb60be42dcf773f42553c737db92f92b4efcfd8e`.
- Product workstream: `WS-42060877a3514cbbb41263d384c8b505`.
- Release workstream: `WS-733cdafe58194d83afc04b92839bfb72`.
- Production path: `release166-a9e038e`.
- Published: `2026-08-23T17:11:30Z`.
- Static rollback: `release165-5b1735c`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **GO**. G10 has P0=0/P1=0/P2=0 after three independent product
  reviews, production browser/hash proof and an executed release165 rollback
  plus forward restore.

## Outcome

The configurator now keeps its visible privacy promise: a visitor's contact,
consent and attached files remain only in the current tab memory. Legacy draft
keys remove old name/contact residue on the next load while retaining safe
working answers.

If a reload removes a required in-memory file, every route to Contact returns
to Materials instead of creating an incomplete high-intent request. The
visitor sees and focuses one explanation, keeps the selected scope, deadline,
cart and price, and can continue by attaching the material again or writing at
least 40 characters. This changes no price, offer, route, backend, admin or
Analytics contract.

## Release proof

- Focused 4/4, related 85/85, public 596/596, backend 31/31 and Brain 39/39
  passed. Three independent exact-candidate reviews reported P0=0/P1=0/P2=0.
- Two public builds are byte-identical: 353 files / 25,054,477 bytes, digest
  `dbec7e39…552`.
- Immutable tree: 356 files / 25,064,648 bytes, full manifest
  `79f51ae8…bfd`; public parity is complete and owner/mode drift is zero.
- Exactly `configurator.html` differs from release165. Repository, immutable
  tree and live HTTP agree at `295b2b3d…8a6e`; server-owned files stayed exact.
- Production Chromium at 360 light and 390 dark proved visible/focused recovery,
  exact practice-support scope and 14,000–19,500 RUB orientation, one disabled
  50 px primary, reattachment and 39/40 -> 40/40 recovery, zero overflow,
  zero persisted synthetic-contact markers, zero POST and zero console errors.
- External and VPS smoke passed 14/14 after activation, 14/14 during the
  executed release165 rollback and 14/14 after final forward restore.
- Service stayed active, Nginx stayed valid, SQLite stayed `ok`, temporary
  pointer residue was zero and no backend process was restarted.

Exact storage, geometry, hash, immutable-tree and rollback observations are in
`E-1026`.

## Live result and boundary

Production `current` and compatibility `dist` resolve to
`release166-a9e038e`; `previous` resolves to `release165-5b1735c`. The rollback
backup is
`/root/site-backups/release166-private-checkpoint-20260823T171035Z`.

No production form was submitted and no client contact, file, order, message,
payment or analytics row was read or changed. The mechanism removes a trust and
scope-continuity defect; organic conversion and revenue uplift are not claimed.

# REL-0175 — честная воронка и устойчивый мобильный переход

- Date: 29 August 2026, Europe/Moscow.
- Authoritative implementation:
  `dcbef91b604a8eec9a2e7a67439eee4069d77222`.
- Workstream: `WS-36779b968bd0436f926cc4dc532813c6`.
- Outcome: `OUT-006`.
- Final activation: `2026-08-29T21:52:23+03:00`.
- Verdict: **GO**. Analytics and mobile: P0=0/P1=0/P2=0;
  SQLite/release: P0=0/P1=0.

## Outcome

The consented funnel now distinguishes real `first_input`, a request
`submit_attempt` and server-confirmed `submit_success`; render-time
`config_step_1` is no longer called input. Authenticated owner devices and
session QA are excluded from first-party events, strict attribution and Yandex
Metrika before the shared runtime starts. Owner preview remains GET-only and
zero-storage. Ordinary owner confirmation drains older deletion proofs in
order and does not rewrite the visitor's consent choice.

A public single-flight guard keeps the first configurator activation native,
shows immediate accessible busy state and blocks rapid repeat taps before a
second navigation or telemetry intent. All 87 measured pages carry one atomic
guard/attribution/analytics cache wave. Promo, prices, retention economics,
drafts, deposits, Salon+, referrals and Kladovaya did not change.

## Verification

- Public 636/636; backend 129 with two expected skips; Brain 39/39; strict
  validation green; focused final set 68/68.
- Two deterministic public builds: 359 files / 26,250,963 bytes, manifest
  `5fc8f6e2…cdc8`.
- Immutable production `release175-dcbef91`: 362 files / 26,261,134 bytes,
  manifest `db91cbce…58e0`, zero owner/mode/path/symlink drift.
- Production Chromium 390×844: zero overflow, native configurator navigation;
  the consented QA exclusion generated zero Analytics/Yandex request and no
  attribution storage.
- External and VPS GET-only smoke passed 14/14 on initial activation, static
  rollback, backend-contract rollback and final forward.
- Final PID `828640`, `NRestarts=0`; Nginx valid; SQLite WAL,
  `quick_check=ok`, FK=0; release-window lock/busy/traceback/critical count 0.

The first trustworthy sample begins after this release. The immediate aggregate
still contains 12 historical `config_step_1` events that cannot be reclassified
as input; post-release events were zero at the final readback. No conversion or
revenue uplift is claimed.

## Rollback

Static rollback is immutable `release173-c245da0`; it was executed and
forward-restored. Backend rollback changes only contract 2.4
`c88146bc…c069` to 2.3 `e05bfd4b…1f99` and restarts the service; it too was
executed and forward-restored. Never run the full old analytics installer or
restore SQLite for REL-0175.

Backups:

- `/root/site-backups/release175-analytics-truth-20260829T184853Z`;
- `/root/salon_bot/backups/analytics-contract-v24-20260829T184853Z`;
- `/root/salon_bot/backups/analytics-contract-v24-20260829T185222Z`.

Exact chronology, hashes, protected-preview behavior and measurement limits are
in [`E-1039`](../evidence/E-1039.md).

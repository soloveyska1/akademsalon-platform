# REL-0178 — честная аналитика ввода в актуальной заявке

- Date: 31 August 2026, Europe/Moscow.
- Authoritative implementation:
  `7d74c64a80753943b62ab6a7b5b914b34bfdeaca`.
- Workstream: `WS-c8e25a769cf84a39b19d6add8b66fefa`.
- Outcome: `OUT-006`.
- Final forward and corrected-series boundary:
  `2026-08-31T01:59:41+03:00`.
- Verdict: **GO**, P0=0/P1=0 after two independent final reviews.

## Outcome

The current dynamic wizard now records one consented `first_input` event on the
first real field change. Route clicks do not count as input, repeated changes do
not duplicate the event and no form field or value is sent.

The admin dashboard adds two aggregate diagnostic cards: meaningful continuation
after a real configurator opening and correct route-specific contact-step reach.
They do not replace the strict conversion funnel.

Prices, deposits, promo, visible customer design, form submission and historical
analytics rows are unchanged.

## Verification

- Public 638/638; backend 130/130 with two expected skips; focused JavaScript
  21/21; Brain 39/39; strict validation and clean diff check.
- Dynamic current-wizard Chromium runtime proves 0 before field input, 1 after
  first input and still 1 after repeated input.
- Admin Chromium smoke at 390/1024/1440 px is free of overflow and CSP errors.
- Two reproducible builds are byte-identical; independent reviews return GO.
- Exact public/filesystem hashes match after activation, an executed backend and
  static rollback, and final forward restore.
- Service, public health, Nginx, SQLite WAL/integrity and post-forward journal
  checks are green.

Full evidence and methodological boundaries are in
[`E-1042`](../evidence/E-1042.md).

## Production and rollback

`current` and `dist` point to `release178-7d74c64`; `previous` points to
`release177-2a6ef72`. Backend backup:
`/root/salon_bot/backups/analytics-input-truth-20260830T225808Z`.

Rollback to release177 plus both backed-up backend files was executed and
hash-checked, then REL-0178 was restored. SQLite must not be restored for this
rollback.

## Measurement boundary

Historical `first_input` is undercounted and is not backfilled. Compare this
event only from `2026-08-31T01:59:41+03:00` forward. Existing aggregate route
engagement diagnostics may include earlier correctly recorded click events.

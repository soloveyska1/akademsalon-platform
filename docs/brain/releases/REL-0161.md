# REL-0161 — выбранный объём практики не теряется после цены

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `1c5f0eed53da5ab894c06545ae80806790fb1d8c`.
- Product result: `34e10367eac4f7451b56bbe01753ff00e9dffce2`.
- Workstream: `WS-f9ec27f9f3b24bfe81bd24b1d77f0b58`.
- Production path: `release161-1c5f0ee`.
- Published after forward restore: `2026-08-23T01:43:55Z`.
- Static rollback: `release160-135cb45`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **GO**. G10 has P0=0/P1=0/P2=0 after exact source/build/hash
  proof, three independent reviews, live browser, two-vantage smoke and an
  executed static rollback/forward.

## Outcome

The three practice-report scopes introduced in release160 now remain distinct
after the visitor leaves the price selector. Diagnostic, editing and staged
support keep their own result code, scope, required inputs, deliverables,
inclusions and exclusions through the configurator, cart/request and
prepayment specification.

The first live screen after selection now repeats the exact purchased scope:

- written diagnostic: 2,500–3,500 RUB, a mismatch map without document edits;
- editing of a ready factual package: 8,000–11,000 RUB, visible Word edits,
  program check and a signatures/applications checklist;
- supplied-material staged support: 14,000–19,500 RUB, a requirements map,
  agreed document versions and a final completeness checklist.

The 14,000 RUB route no longer degrades into a generic project "from zero".
Its request and specification remain A1 supplied-material editing, while a
real generic `topic+support` route remains A2. Work starts only after the full
listed input set is received. The Otisk composition, one-primary rule,
light/dark themes, authorship boundary and existing prices were preserved;
there is no discount, fake scarcity, grade promise or uplift claim.

## Release proof

- Site 589/589; backend 31/31; Brain 39/39; focused practice/cart/specification
  contracts 25/25; strict validation and diff/syntax checks green.
- Two public-client builds are byte-identical: 353 files / 25,030,042 bytes,
  digest `00cc9703…6952`.
- Immutable tree: 356 files / 25,040,213 bytes, manifest
  `a455a53b…23f4`; source parity 353/353 and owner/mode drift 0.
- Exactly five public files differ from release160; repository/tree/HTTP
  SHA-256 agree for all five. Three server-owned files were preserved exactly.
- Production Chromium at 390/1440 in light/dark has overflow 0 and console
  errors/warnings 0. All three routes show their exact title, output, price
  range, upload request and link back to the public selector.
- External and VPS smoke passed 14/14 after activation, 14/14 in the executed
  release160 rollback and 14/14 after final forward restore.
- Backend, database, Analytics, Nginx and security headers were unchanged; the
  backend service was not restarted.

Exact hashes, route assertions, browser observations and rollback chronology
are in `E-1022`.

## Live exact result and rollback

`current` and compatibility `dist` resolve to `release161-1c5f0ee`;
`previous` resolves to `release160-135cb45`. Service is active, Nginx syntax is
valid and SQLite is `ok`. Static rollback is the paired pointer switch to
release160; no backend restore, migration or restart is part of this release.

An owner-only protected preview of the same product source remains available
as Sites version 37 before public activation.

## Residual boundary

The acquisition source of the lost lead is still unknown because admin
analytics were intentionally outside the requested product scope. Release
correctness proves continuity, not conversion uplift. The next bounded product
candidate is a compact result passport linked to the existing dissected demo
specification; it must remain an example, not a fixed package or invented
practice case.

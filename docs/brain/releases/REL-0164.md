# REL-0164 — мобильный выбор практики сохраняется после Browser Back

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `1ea81c6825c02b94f8a8dff84a9186a8534b4265`.
- Verified product result: `89ddeed5744c428a09d0096b1b37621538a3ff72`.
- Product workstream: `WS-762941dd7c9e40ed98f88db36f380122`.
- Release workstream: `WS-dee7b3e6ff1d43e4aa70d2135f4664b4`.
- Production path: `release164-1ea81c6`.
- Published after forward restore: `2026-08-23T05:04:30Z`.
- Static rollback: `release162-977c1f6`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **GO**. G10 has P0=0/P1=0/P2=0 after the failed release163 was
  rolled back, the Browser Back state was fixed, three independent reviews
  passed, and release164 completed live browser and rollback/forward proof.

## Outcome

On the practice page, the fixed thumb-reachable mobile primary now follows the
same allowlisted scope as the two page continuations:

- written diagnostic from 2,500 RUB;
- editing of a ready factual package from 8,000 RUB;
- staged supplied-material support from 14,000 RUB.

An existing configurator draft still owns the initial `Черновик` action. A
real pointer tap or Space activation on a scope intentionally replaces that
action without deleting the saved draft. If the browser restores a non-default
radio after visiting the fictional specification, the restored checked state
is distinguished from the HTML default and all three continuations are
reconciled to the same scope on `pageshow`.

Prices, offer composition, Otisk layout, configurator, request/specification,
backend, admin and Analytics were not changed. The mechanism preserves the
visitor's selected commercial intent; it does not prove conversion uplift.

## Release proof

- Public product 592/592; backend 31/31; Brain 39/39; strict validation and
  diff checks green. Three independent exact-result reviews report
  P0=0/P1=0/P2=0.
- Two public-client builds are byte-identical: 353 files / 25,041,362 bytes,
  digest
  `7915d08510de3e24eee8dd95bd49818dc8b52e73619245c10d65148bb789be97`.
- Immutable tree: 356 files / 25,051,533 bytes, full manifest
  `c3050fffb6d1a55b5132cb0fe876e16d5887ba16fe89385887ff754c564e8053`;
  source parity 353/353 and owner/mode drift 0.
- Exactly `otchet-po-praktike.html` differs from release162. Repository,
  immutable tree and live HTTP agree on SHA-256
  `3292958b15940636f012a51c4c3d0c6cf4e5d610f6f7e3a5c7936e6b4be4585b`.
  The live CSS and three server-owned files stayed byte-identical.
- Production Chromium proved fresh diagnostic/editing/support handoffs,
  saved-draft pointer and Space activation, one live announcement and zero on
  repeat, support -> fictional specification -> Browser Back parity, 360/390
  light/dark overflow 0 and a hidden desktop dock at 1440. Console errors and
  warnings were 0.
- External and VPS smoke passed 14/14 after activation, 14/14 in the executed
  release162 rollback and 14/14 after final forward restore.
- Service stayed active, Nginx stayed valid, SQLite stayed `ok`, and no backend
  process was restarted.

Exact hashes, browser observations, failed-release chronology and rollback
commands are recorded in `E-1024`.

## Live result and residual boundary

`current` and compatibility `dist` resolve to `release164-1ea81c6`;
`previous` resolves to `release162-977c1f6`. The inactive release163 artifact
is retained only as evidence and is not in the pointer chain.

No form was submitted and no production contact, order, client file, message,
payment or analytics record was read or changed. The next bounded public-site
question remains primary-action reachability while the mobile software
keyboard is open; this release does not claim that issue is solved.

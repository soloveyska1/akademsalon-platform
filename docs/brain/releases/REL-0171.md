# REL-0171 — сентябрьский вход и безопасная экономика

- Date: 25 August 2026, Europe/Moscow.
- Canonical product source:
  `ffa2421ce9b3aed62b3c508a210fa50a2bb1e438`.
- Entry implementation: `1ee9533df1d677c34b44c95bf8e13ec9a7cda5e3`;
  economic implementation: `4acd0e623f5b2a5e2efd6926f799b71032234c4d`.
- Workstreams: `WS-2ecb95c1742441bc82768b6c38edcb95`,
  `WS-e92c109191de49b9b1f13b0f1ba1d485`,
  `WS-69dc4870a4f44aafa9e2c93fc7c5425a`.
- Outcomes: `OUT-004`, `OUT-008`.
- Production artifact: `release171-ffa2421`; final forward publication:
  `2026-08-25T13:12:44+03:00`.
- Verdict: **GO**, P0=0/P1=0 for this bounded release after exact combined
  regression, production Chromium, two-vantage smoke and executed backend plus
  static rollback-forward.

## Outcome

New visitors receive the reviewed branded `ПЕРВЫЙЛИСТ` sheet through a
33,652-byte WebP with PNG fallback. Returning footprints remain suppressed and
owner preview remains labelled and non-redeemable. The home/configurator entry
describes the real three steps and conditional response window without an
unsupported price or response promise.

Deposits remain open. The public 8/10/12/15 ladder and wallet CTA are intact;
principal is refundable and benefit is earned from actual net use. Deposit
value and ordinary cashback are best-of, only uplift is held for acceptance
plus 14 days, and partial refunds return exact unused principal. The referral
contract is one fixed 200-point reward after the first fully paid qualifying
order. Eleven persistent database guards make legacy and inconsistent money
writes fail closed.

No CSS or cabinet page changed. REL-0171 does not claim that separate legal,
SEO, 320px overflow or Browser Back debt is complete.

## Verification

- Combined public 612/612, backend 99/99 with one expected local skip, Brain
  39/39, strict validation, compile/build and diff checks passed.
- Two deterministic builds: 357 files / 26,210,824 bytes, manifest
  `4a3c2f22...053`; immutable production tree with three retained server files:
  360 files / 26,220,995 bytes.
- Final backend state is `after`, `quick_check=ok`, eleven exact triggers and
  four invariant counters at zero. Settings are `earned-v2:on`,
  `earned-v2:open`, `fixed-200:on`.
- Final PID `557663` has `NRestarts=0`; the 442-second final journal window has
  zero lock/traceback/scheduler/ERROR/CRITICAL records. Nginx is valid.
- Operator-network and VPS GET-only smoke passed 14/14 after activation, on the
  real rollback and again after forward restore.
- Production Chromium at 390x844 and 1440x900 loaded the exact 960x720 WebP,
  kept root width equal to viewport and reported zero console errors/warnings.
  The live deposit page showed 8/10/12/15, no pause copy and the existing wallet
  CTA; no production form or money mutation was performed.

## Rollback

Static rollback is immutable `release169-51f3556`; it was executed and then
forward-restored. Backend rollback uses the pinned installer `--disable`: it
closes only new top-ups/referral grants while the V2 adapter continues serving
existing principal, paid holds and earned rewards. Never restore the SQLite
snapshot as routine rollback.

Production backup:
`/root/site-backups/release171-september-20260825T100556Z`; source pre-images:
`/root/salon_bot/backups/economic-v2-20260825T100618364976Z`; online emergency
copy:
`/root/salon_bot/backups/release171-pre-20260825T100556Z/salon.db`.

Exact chronology, hashes and safety boundaries are in
[`E-1035`](../evidence/E-1035.md). Publication does not prove conversion,
revenue or profit uplift.

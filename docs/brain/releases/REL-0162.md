# REL-0162 — цена сопровождения подкреплена проверяемым составом

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `977c1f6a52a38c59ea5bbfc9c93910b8e615495c`.
- Product result: `29376844dc9691f0f9f8f40dbd83ebcdc0f2d1ee`.
- Workstream: `WS-22c20caf674f4e478575eb974368db42`.
- Production path: `release162-977c1f6`.
- Published after forward restore: `2026-08-23T02:55:00Z`.
- Static rollback: `release161-1c5f0ee`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **GO**. G10 has P0=0/P1=0/P2=0 after exact source/build/hash
  proof, three independent reviews, live browser, two-vantage smoke and an
  executed static rollback/forward.

## Outcome

The 14,000 RUB practice-support choice now expands one compact result passport
inside the selected option. It names the real client inputs, four checkable
outputs, authorship boundary, exclusions and the exact moment when scope and
price are fixed. It also links to the existing dissected specification while
explicitly labelling that example as fictional.

The price meaning is no longer ambiguous: 14,000 RUB is the lower orientation
for the complete listed composition, not only the requirements map delivered
first. The future specification fixes the total and the stage split before
payment; work outside it is estimated separately. The visitor may stop before
launching a later stage.

The passport is absent for the 2,500 RUB diagnostic and 8,000 RUB editing
choices, so it does not make the smaller scopes look incomplete. The existing
native radios and two continuations stay synchronized, including after opening
the specification and using Browser Back. Prices, configurator, request,
backend, Analytics, admin and the Otisk visual language were not changed.

## Release proof

- Site 590/590; backend 31/31; Brain 39/39; strict validation and diff checks
  green. Three independent final reviews report P0=0/P1=0/P2=0.
- Two public-client builds are byte-identical: 353 files / 25,039,823 bytes,
  digest `c7a7fff…dd7c`.
- Immutable tree: 356 files / 25,049,994 bytes, manifest `20cd81dc…a70`;
  source parity 353/353 and owner/mode drift 0.
- Exactly two public files differ from release161; repository/tree/HTTP
  SHA-256 agree for both. Three server-owned files were preserved exactly.
- Production Chromium at 390/1440 in light/dark has overflow 0 and console
  errors/warnings 0. Conditional visibility, exact routes, 54 px disclosure
  target, keyboard radios, two-column desktop layout and Browser Back state all
  passed.
- External and VPS smoke passed 14/14 after activation, 14/14 in the executed
  release161 rollback and 14/14 after final forward restore.
- Backend, database, Analytics, Nginx and security-header hashes were unchanged;
  the backend service was not restarted.

Exact hashes, browser observations and rollback chronology are in `E-1023`.

## Live exact result and rollback

`current` and compatibility `dist` resolve to `release162-977c1f6`;
`previous` resolves to `release161-1c5f0ee`. Service is active, Nginx syntax is
valid and SQLite is `ok`. Static rollback is the paired pointer switch to
release161; no backend restore, migration or restart is part of this release.

An owner-only protected preview of the same canonical source remains available
as Sites version 38.

## Residual boundary

This release improves the clarity and defensibility of the offer; it does not
prove conversion uplift. The acquisition source of the rejected lead remains
unknown because admin and statistics were intentionally outside the requested
scope. Measure only a sufficient organic consented sample before making a
causal claim.

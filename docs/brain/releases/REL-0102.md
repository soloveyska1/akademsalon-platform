# REL-0102 — OUT-007 search and catalogue clarity release

- Source commit: `e2f76c3d71c82169f52e3c94874424e150cc54d3`
- Production path: `release102-e2f76c3d71c8`
- Published: 3 August 2026
- Rollback: `release101-b9837a34c4e`
- Verdict: published, but not G10 GO. A post-publish P1 search-trigger gap at
  921–1240 px requires the bounded release103 closure.

## Scope

The release is the deterministic public build of exact integrated OUT-007. It
publishes the shared search presentation, deterministic findability and stable
states described in `E-1009`, while preserving the protected catalogue routes,
prices, schema and saved-intent contract.

No production submit, OAuth, payment, upload, analytics-consent mutation,
client-data access, deletion or local model was part of publication or proof.

## Pre-publish gates

- Exact canonical source:
  `e2f76c3d71c82169f52e3c94874424e150cc54d3`; verified OUT-007 result:
  `1c9c7a382de13e9c2877221034269144eab702a5`.
- Product regression: 500/500; Brain: 39/39; strict validation and
  deterministic build checks passed.
- Public deploy selection: 337 payload files. The server release preserves two
  server-owned extras — `.indexnow-key` and
  `analysis/growth-baseline-2026-07-24.ipynb` — for 339 files total.
- Pre-release backup:
  `pre-release102-e2f76c3d71c8-20260803T062310Z.tar.gz`, 19,241,636 bytes,
  SHA-256
  `5eba3e7da2df9acdef692b6d7c3c6ef2ed72d90c979c9dfe52ba02111cc7c7ca`.

## Production evidence

- Nginx `current` and compatibility `dist` resolve to
  `release102-e2f76c3d71c8`.
- The inactive staging tree was checked as 337/337 exact payload hashes; both
  preserved extras were exact. The live release has the expected `www-data`
  ownership, directory mode 755 and file mode 644.
- Checked-in GET/HEAD-only production smoke passed 14/14 on the VPS and 14/14
  externally through the stable `en0` route after the final forward restore.
- Key release102 hashes include shared chrome CSS
  `c21a018d7f9aa510efee512b5857e9dabe7d8fc3dd1dc3b36db0b694a5df1b0a`,
  home CSS
  `def1bcfd2e22a70e00f8b763fde41fcb2836f5b7a4ff7518eb7cbb869de502ab`,
  `index.html`
  `03d62614710d07eb628bffac3c146260be4b2dc8952cefb09041be0d85b5609e`
  and catalogue JS
  `a18030aad81f2afada2730c705d91aa8fc6b55273e324547469faf73844eb7cc`.

## Rollback proof

Rollback was executed with a server-side switch and forward restore. Both
symlinks resolved to `release101-b9837a34c4e`; health was green and catalogue
JS became
`48ed111f36b871f81ad0d890f75bd095294ed161a4db84539992a6251c4dbf3b`.
Both symlinks were then restored to release102; health was green and the
catalogue JS returned to
`a18030aad81f2afada2730c705d91aa8fc6b55273e324547469faf73844eb7cc`.

Two earlier script attempts did not leave production in an intermediate state:
one stopped before mutation because of a shell-local variable error; the other
briefly selected release101 and its trap restored release102. The corrected
rollback/forward procedure above then completed successfully.

## Post-publish P1 and superseding boundary

The final release102 browser matrix found zero visible pointer search triggers
on home, services and dashboard between 921 and 1240 px. Release101 contains the
same release97 hide rule, so emergency rollback does not repair this defect.
The issue is P1 under G5/G6 and prevents a truthful G10 GO/P1=0 verdict for
release102 even though publication, health, smoke and rollback succeeded.

The bounded release103 candidate `e97a66a` adds one late shared-CSS rescue,
preserves JS/search/catalog semantics and passes 501/501 plus a 42/42 WebKit
matrix. Its durable evidence is carried by the release103 product workstream
until integration. Release103 must use a separate
`production:deploy` workstream and may be called GO only after live browser,
smoke and executed release102 rollback/forward proof.

## Operations observations

SSH is reliable only when explicitly bound to `en0`; the default `utun6` route
can time out before a session. An initial inactive staging directory
`.release102-e2f76c3d71c8.staging` contains Apple `._` sidecars. It is not live
and was intentionally not deleted without a separate cleanup plan.

# REL-0165 — primary остаётся доступным над мобильной клавиатурой

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `5b1735c2c959ce63733b270259f74965d2fe46d3`.
- Verified product result: `8adcfb4543f7388f7b03403007771d63dae1e19f`.
- Product workstream: `WS-2f182483670947249a23af6655d28636`.
- Release workstream: `WS-e3df0fa5ee164a05bc6b28271da87040`.
- Production path: `release165-5b1735c`.
- Published: `2026-08-23T06:50:40Z`.
- Static rollback: `release164-1ea81c6`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **GO**. G10 has P0=0/P1=0/P2=0 after two independent
  exact-result reviews, production browser/hash proof and an executed
  release164 rollback plus forward restore.

## Outcome

When the software keyboard opens in the mobile configurator, the existing
current-step primary becomes one compact shelf above the keyboard. The focused
field, validation reason and action remain simultaneously reachable; the
40-character boundary enables the same primary without blur. Closing the
keyboard restores the full taskbar.

This is a page-local state of the existing control, not a second CTA. The
mechanism neither changes a route nor submits a form. Consent keeps precedence,
desktop geometry is unchanged, and the exact practice-support scope retains
its 14,000–19,500 RUB orientation. Prices, offer composition, submit contract,
backend, admin and Analytics were not changed. Reachability is proved;
conversion uplift is not claimed.

## Release proof

- Public product 592/592; backend 31/31; Brain validation and diff checks
  green. Chromium and WebKit focused keyboard journeys passed 3/3 each at
  320/390/430. Two independent reviews report P0=0/P1=0/P2=0.
- Two public-client builds are byte-identical: 353 files / 25,048,287 bytes,
  digest
  `51f755708d2537a2bf6904132604427c501378cdb5ebad06949fe128534d52e7`.
- Immutable tree: 356 files / 25,058,458 bytes, full manifest
  `7ea25e87ab5863333cebed04e6a90c9f5f574492084fd9bafafc3d01f7226e4c`;
  source parity is complete and owner/mode drift is zero.
- Exactly `configurator.html` and `assets/css/configurator-journey.css` differ
  from release164. Repository, immutable tree and live HTTP agree at
  `7f99eda2…0cd4` and `d474c38e…b1b`; server-owned assets stayed unchanged.
- Production Chromium proved 39/40 disabled, 40/40 enabled without blur, one
  hit-testable primary, exact support scope/price, contact handoff, zero root
  overflow and no POST at 360 light and 390 dark. The reviewed source was also
  exercised with a real iOS 26.4 software keyboard before publication; live
  iOS loaded the exact production route and live hashes bind production to the
  reviewed HTML/CSS.
- External and VPS smoke passed 14/14 after activation, 14/14 during the
  executed release164 rollback and 14/14 after final forward restore.
- Service stayed active, Nginx stayed valid, SQLite stayed `ok`, temporary
  pointer residue was zero and no backend process was restarted.

Exact geometry, hashes, immutable-tree proof and rollback observations are in
`E-1025`.

## Live result and residual boundary

Production `current` and compatibility `dist` resolve to
`release165-5b1735c`; `previous` resolves to `release164-1ea81c6`. The rollback
backup is
`/root/site-backups/release165-keyboard-shelf-20260823T064936Z`.

The broad legacy 42-case mobile runner still contains unrelated historic
wait/CORS failures and a 320 px tariffs overflow. They are not used as evidence
for this bounded configurator path; its isolated Chromium/WebKit journeys,
production browser observations and full public regression are green.

No production contact, order, client file, message, payment or analytics row
was read or changed. No form was submitted.

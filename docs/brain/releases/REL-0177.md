# REL-0177 — мобильные этапы без обрезания

- Date: 29 August 2026, Europe/Moscow.
- Authoritative public implementation:
  `2a6ef72a765ed6962fbd51ef164b9aa005b28047`.
- Workstream: `WS-b635c17c89744c778cbb4d12b930bbca`.
- Outcome: `OUT-008`.
- Final forward readback: `2026-08-29T23:32:10+03:00`.
- Verdict: **GO**, P0=0/P1=0/P2=0 after independent review and repeat tests.

## Outcome

The shared four-stage bridge becomes a clean 2x2 grid below 360 px. All stages
stay inside 16 px side gutters, including intermediate narrow widths and 200%
text inflation. At 360 px and above the existing layout is unchanged.

Only bounded CSS and its cache URL in 89 consumers changed. Prices, deposits,
promo, copy, routes, analytics, runtime JS and backend are untouched.

## Verification

- Failing-first contract; focused 15/15; public 638/638; backend 129 with two
  expected skips; Brain 39/39; two byte-identical builds.
- Chromium/WebKit, light/dark, 320–1440 widths, intermediate boundary,
  forced-colors, keyboard focus and true computed-font 200% reflow checked.
- Exact production artifact: 362 files / 26,260,957 bytes; 90 local/live
  filesystem hashes match; 89 HTTP body hashes plus one unchanged 301 verified
  at activation, executed rollback and final forward restore.
- Final external/VPS smoke 14/14. Production browser at 320/390 has zero
  overflow, console errors or non-GET requests.
- Backend stayed on PID `828640`, NRestarts=0; health/Nginx/release-window
  journal checks are green. No database operation or service restart.

Exact proof and validation boundaries are in [`E-1041`](../evidence/E-1041.md).

## Rollback

`current` and `dist` → `release177-2a6ef72`; `previous` →
`release176-9d44d99`. Static rollback to release176 was executed and hash-checked,
then release177 was restored. No backend or database action is needed.
Pointer/target-hash backup:
`/root/site-backups/release177-case-bridge-20260829T2022Z`.

# REL-0176 — честный ценовой сигнал дисциплинарных страниц

- Date: 29 August 2026, Europe/Moscow.
- Authoritative public implementation:
  `9d44d99f66e4174960777a579d203089f0758f7d`.
- Workstream: `WS-194971f3069a4586b480cc95eac95543`.
- Outcome: `OUT-007`.
- Final activation: `2026-08-29T22:38:48+03:00`.
- Verdict: **GO**. Final independent reviews: P0=0/P1=0/P2=0.

## Outcome

Nine discipline landing pages no longer tell search systems about hidden
offers from 5,000 RUB while showing a higher entry orientation to a visitor.
Each page publishes one Service Offer whose minimum exactly matches the visible
`от N ₽` value. The structural test covers all nine pages and forbids any
parallel hidden Offer or AggregateOffer.

No client-facing price, copy, CTA, design, CSS, runtime JavaScript,
configurator route, promo, deposit, analytics or backend behavior changed.

## Verification

- Focused 8/8; public 637/637; backend 129 with two expected skips; Brain
  39/39; strict validation, JSON parsing, XML and diff checks green.
- Two deterministic public builds: 360 files / 26,248,863 bytes, manifest
  `07f9c97c...89267`.
- Immutable production `release176-9d44d99`: 362 files / 26,331,823 bytes,
  manifest `7dfbac84...64fab3`, zero owner/mode/symlink drift.
- Final external GET smoke 14/14 and exact public hashes for all ten published
  files passed.
- Static rollback to `release175-dcbef91` was executed and hash-verified, then
  release176 was forward-restored and reverified.
- Backend stayed on PID `828640`, `NRestarts=0`; Nginx, health, SQLite and the
  release-window error scan are green.

Correct markup does not guarantee a rich result or ranking change. Recrawl,
qualified organic traffic and conversion effect are future measurements, not
claims of this release. Exact proof is in [`E-1040`](../evidence/E-1040.md).

## Rollback

Static rollback is immutable `release175-dcbef91`. Switch `current` and `dist`
to it atomically; no backend restart or database action is required. Pointer
and target-hash backup:
`/root/site-backups/release176-seo-pricing-20260829T193651Z`.

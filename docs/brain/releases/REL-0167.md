# REL-0167 — восстановление Telegram-входа в админку

- Date: 24 August 2026, Europe/Moscow.
- Static production source: unchanged
  `a9e038ed62c126e3d54290cfda547c50bd290772`.
- Static production path: unchanged `release166-a9e038e`.
- Backend implementation:
  `cc9cfed21c3ebebba386d6962954fc85f8a87df7`.
- Backend before:
  `6c30cf2f02fcda1247131cc87078bb643307b5746880825bf7b6bec4fce55c05`.
- Backend result:
  `14a45362e9ce17416c552557f4610546ce23549a4fc35dbbccc46d9e624448ee`.
- Workstream: `WS-bbcc4726ff154314b3b93eed77ac4b13`.
- Verdict: **GO**, P0=0/P1=0/P2=0 after two independent final reviews,
  production browser proof and an executed backend rollback plus forward
  restore.

## Outcome

An expired or revoked HttpOnly session cookie no longer traps the admin entry
screen in CSRF `403`. Exact `POST /api/auth/start` may discard that invalid
cookie and invoke the unchanged anonymous Telegram bootstrap. A valid session
still passes the full exact-Origin, double-submit and database-bound CSRF
contract; every other unsafe route remains fail-closed.

No static page, visual, admin workspace, price, public form, order contract or
Analytics v2 schema changed.

## Release proof

- focused auth/installer 8/8, full backend 39/39, public 596/596 and Brain
  39/39 passed;
- exact source/candidate hashes and Python compile passed;
- two independent final reviewers reported P0=0/P1=0/P2=0;
- stale-cookie production matrix returned auth-start `200`, protected order
  `403` and non-POST auth-start `403`, with exact deletion attributes for both
  stale auth cookies;
- the originally failing browser tab reached `Подтвердите в боте…` and exposed
  the normal Telegram continuation;
- service active, Nginx valid and read-only SQLite quick-check `ok` after the
  initial apply, executed rollback and final forward restore.

Exact diagnosis, test matrix, hashes, backups and the transient restart window
are recorded in `E-1027`.

## Live result and rollback

Production backend is on exact result `14a453…48ee`. The final rollback copy is
`/root/salon_bot/backups/admin-auth-recovery-20260823T220441760076Z`; restoring
it through the pinned installer returns exact before-hash `6c30…55c05`, after
which `salon-bot-v2.service` must be restarted and health/auth smoke repeated.

The admin tab is intentionally left at the existing `Открыть бота` step. The
user still confirms the one-time login in Telegram; the release does not claim
that authenticated access was completed on their behalf.

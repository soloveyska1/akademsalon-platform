# REL-0172 — умное удержание и безопасная передача замечаний

- Date: 25 August 2026, Europe/Moscow.
- Canonical product source:
  `df007e40399f47a26bd3903b4702fbcc74755ab4`.
- Implementation result: `925309b2a5380457a336c892ef89fac564898239`.
- Product workstream: `WS-9bec44129f3e4c60a975399edea4e96c`;
  publication workstream: `WS-539a8acc8cf64725b225a5b4b98c8eb5`.
- Outcome: `OUT-006`.
- Production artifact: `release172-df007e4`; final forward publication:
  `2026-08-25T15:24:02+03:00`.
- Verdict: **GO**, P0=0/P1=0 for the bounded smart-rescue release after exact
  regressions, independent economics/architecture/UX reviews, production
  browser inspection, two-vantage smoke and executed static rollback-forward.

## Outcome

An unfinished configurator exit now asks one finite reason. Only `Цена выше
ожиданий` requests the existing qualified retention offer: 10% from a 5,000 RUB
order, capped at 2,500 RUB for 72 hours. Missing materials, unclear composition
and a deadline question return the visitor to a relevant existing path without
issuing a discount. Browser Back or an edge swipe closes the rescue sheet before
changing a wizard step.

Returning users remain fully suppressed. The labelled owner preview at
`/configurator.html?offer_preview=retention` reproduces the client presentation
but cannot claim an offer, navigate through its action, write promo state or
change statistics.

The supervisor-remarks dossier transfers 40–800 characters to the configurator
through a ten-minute same-tab handoff. The URL contains only a route code. An
existing safe draft wins only after an explicit choice; starting a new route
imports the remarks into the ordinary local draft. Legacy private handoff keys
are scrubbed and throwing storage access fails closed.

No backend, bot, database, tariff, order schema, deposit page/economics,
Salon+, referral or Kladovaya artifact changed. Deposit issuance remains open.

## Verification

- Public 621/621; backend 99/99 with one expected local production-only skip;
  Brain 39/39; focused rescue/remarks 15/15.
- Ordinary mobile Chromium/WebKit 12/12 and targeted mobile matrix 24/24 at
  320/390/430, including Back, both draft-conflict choices, legacy storage and
  zero-mutation owner preview.
- Two deterministic builds: 358 public files / 26,234,987 bytes, manifest
  `63f2f9ed657e9a128d6c23295345ad5be5f93901be9772a8a1129d95895a00cf`.
- Final immutable release: 361 files, comprising the exact public build and
  three unchanged retained server files; owner, mode, path, symlink and hash
  drift are zero.
- Production browser proof covered owner preview, old-user suppression and the
  remarks route with zero POST. No form, order, contact, file, payment, deposit,
  bonus, message, promo claim or retention grant was created.
- External and VPS GET-only smoke passed 14/14 after activation, during the
  real rollback to REL-0171 and after forward restore.
- Final bot PID is `557663`, `NRestarts=0`; Nginx is valid, SQLite is WAL with
  `quick_check=ok`, and the post-release journal contains zero actual
  `database is locked`, traceback, ERROR or CRITICAL matches.

## Rollback

Static rollback is immutable `release171-ffa2421`; it was executed and then
forward-restored. No backend or database rollback belongs to REL-0172 because
neither was changed. Production backup:
`/root/site-backups/release172-smart-rescue-20260825T121703Z`.

Exact chronology, hashes and privacy boundaries are in
[`E-1036`](../evidence/E-1036.md). Publication proves the mechanics and safety
boundary, not conversion, revenue, contribution margin or profit uplift.

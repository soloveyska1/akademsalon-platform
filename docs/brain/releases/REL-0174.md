# REL-0174 — подтверждённый технический путь заявки

- Date: 26 August 2026, Europe/Moscow.
- Canonical source:
  `d6f1a1b4783d5600abcfe5ecce835c56e5c3bac4`.
- Authoritative implementation:
  `7cf25718e9c8ae39c1e7105b62d3206e4e8196b7`; generated-link hotfix:
  `292784c11aeadfd860e660d9c2f4147e19d74069`.
- Final publication workstream: `WS-dda370b659e74042941fd82fd6de984d`.
- Outcome: `OUT-001`.
- Final activation: `2026-08-26T05:12:08+03:00`.
- Verdict: **GO**, with three independent final reviews at P0=0/P1=0/P2=0.

## Outcome

The authoritative backend now contains a default-off, root-authorized synthetic
plane for proving the bounded technical backend path without client data or real
delivery. One bounded fixture passes API creation, duplicate recovery, isolated
outbox delivery, claim exchange, cabinet list/detail and exact cleanup. The
capability is short-lived and root-owned; ordinary requests never read it.

Independent review stopped the first candidate after finding that a future
generated `order_id` could be hidden from `PRAGMA table_info`. The final runtime
uses `PRAGMA table_xinfo`; VIRTUAL and STORED link repros now fail closed before
deletion. Live inventory found no historical hidden residue. This hotfix changed
only the runtime, its installer hash pin and regression; prices, promo,
eligibility, deposits and public design did not change.

## Verification

- Failing-first generated-link regression, then focused backend 30/30.
- Full backend 129 discovered with one exact environment skip; public 623/623;
  Brain 39/39; strict Brain validation green.
- Final security/trust, economics/eligibility and product/ops reviews:
  P0=0/P1=0/P2=0 after documentary P2 closure.
- Exact production set: installer `031bedbe…d186`, runtime `cba09cf5…44a5`,
  probe `5dd4cb60…452d`, migration `e6500fa4…f8d8`, source post-images
  `b9ac6409…efd6f` and `cb5b2624…a200`.
- Repeated production probe: one order/marker/event/outbox/receipt/session/
  cabinet/claim, zero items/files; duplicate exact; `proof_ready=true`;
  cleanup exact; second cleanup no-op.
- Final aggregate state: active synthetic/outbox/receipt 0, capability absent,
  two opaque cleaned tombstones, schema/link inventory exact 30/30, no hidden
  order links, economic guard unchanged, deposits `earned-v2:open`.
- External and VPS GET-only smoke passed 14/14 before the transition, after
  activation, on the executed rollback and after final forward.
- Final service PID `768912`, `NRestarts=0`; Nginx valid, SQLite WAL,
  `quick_check=ok`, FK violations zero.

The release window had no SQLite lock, traceback or CRITICAL entry. Six transient
upstream Telegram `Bad Gateway` polling messages occurred during one 10-second
interval after the final forward; HTTP/SQLite/PID stayed healthy and polling
recovered without a restart, with a subsequent clean journal window. The
synthetic delivery was isolated and did not address Telegram.

## Rollback

Direct new-installer apply over the former runtime is forbidden and fails
closed. Production used the rehearsed old-installer rollback, then new apply and
idempotence. A real new rollback restored exact source preimages and removed all
three assets while retaining the additive migration and tombstones; external and
VPS smoke stayed 14/14. Forward restore created the final rollback copy:

`/root/salon_bot/backups/out001-synthetic-20260826T021208015769Z`.

No SQLite snapshot was restored. Static production remains
`release173-c245da0` with `release172-df007e4` as previous, so REL-0174 does not
change the public design.

Exact chronology, hashes, probe digests and limitations are in
[`E-1038`](../evidence/E-1038.md). This release proves the bounded technical
path, not organic conversion, external fulfilment quality, revenue or profit.

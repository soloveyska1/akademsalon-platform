# REL-0163 — снят с production после Browser Back P1

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `9f9d5980166ad2fe87f26b31ff2870df6283c497`.
- Workstream: `WS-5e376cfa44f04e4780c89c9eae9762e4`.
- Inactive production path: `release163-9f9d598`.
- Static rollback: `release162-977c1f6`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **NO-GO, rolled back**. A production-only P1 appeared in the
  saved-draft Browser Back path; the release was removed before handoff and was
  never restored forward.

## Intended outcome

The fixed mobile primary was meant to follow the selected 2,500 / 8,000 /
14,000 RUB practice scope. Fresh diagnostic, editing and support routes, the
saved-draft initial guard, pointer/Space activation, live-region idempotence and
360/390 light/dark geometry passed before publication.

## Release-gate failure

Production Chromium with an actual saved support draft reproduced this exact
sequence:

1. select support and see `от 14 000 ₽`;
2. open the visibly labelled fictional specification;
3. use Browser Back.

The browser restored the support radio, status and passport, but the fixed
mobile action was rebuilt as the default editing route
`result=editing&route=page` and returned to the label `Черновик`. The nearest
mobile primary therefore contradicted the visible 14,000 RUB selection. This
was a G10 P1 and blocked release regardless of passing deterministic tests.

## Safe rollback

The paired static pointers were immediately returned to
`release162-977c1f6`; `previous` returned to `release161-1c5f0ee`. External and
VPS GET/HEAD smoke both passed 14/14, the release162 page SHA-256
`7d445798eac5f4d7f75579805b614ce6eb506c1034ce1ae87302667442bdcd80`
was restored, and backend, Nginx and SQLite remained healthy. Final rollback
time was `2026-08-23T04:34:40Z`.

The immutable failed artifact remains inactive for audit:

- 356 files / 25,051,424 bytes;
- page SHA-256
  `022bdfa614c09cae162227be6cd603bc72acf621c352f9f55f3e88893f2f7ce1`;
- full manifest
  `c3090a571d06f64ed62874ad51117b9f3e067f2ddf3019d46a48f7c91ea13cf1`.

It must not be reused as a release base. The bounded fix and successful
replacement publication are recorded in `REL-0164` and `E-1024`.

# REL-0173 — коррекция экрана удержания

- Date: 25 August 2026, Europe/Moscow.
- Canonical product source:
  `c245da0e00ce3a9dfaefbe985b9540672687e1d3`.
- Implementation result: `5ac8bc8f5af40b1d63fb4b9fd3e18413ffef5a27`.
- Product workstream: `WS-08d29437a63b4a6ebd06258dfbab076f`;
  publication workstream: `WS-3acdadf46aad4609bca4c5d7463302c0`.
- Outcome: `OUT-006`.
- Production artifact: `release173-c245da0`; final forward publication:
  `2026-08-25T16:37:26+03:00`.
- Verdict: **GO**, P0=0/P1=0 for the bounded visual correction after exact
  regression, visual/accessibility reviews, production browser inspection,
  two-vantage smoke and executed static rollback-forward.

## Outcome

The unclear-composition outcome now says `Итог уже готов` and stays inside the
retention sheet. Retention headings use a bounded scale; actions are limited to
340/360 px; the primary uses the Salon wax color instead of a full-width black
slab. The owner marker and close remain visible after every reason because only
the sheet's inner scroll is reset. Light, dark, reduced-motion and forced-color
roles remain accessible.

The four decisions and their economics did not change. Only an explicit price
objection can request the existing qualified 10% offer from 5,000 RUB, capped
at 2,500 RUB for 72 hours. Returning users remain suppressed. Owner preview is
labelled, non-redeemable and cannot navigate, persist promo state or change
statistics.

No backend, bot, database, API, price, tariff, deposit, Salon+, referral or
Kladovaya artifact changed. Deposit issuance remains open.

## Verification

- Focused 14/14; public 623/623; Brain 39/39; strict validation green.
- Targeted WebKit/Chromium light/dark/reduced-motion matrix 24/24 across
  320/390/430/568x514/760x650/1440; Chromium forced-colors and dark-hover
  contrast green.
- Two final independent reviews: visual P0=0/P1=0/P2=0; accessibility/
  behavior P0=0/P1=0. The only accepted P2 is pre-existing legacy default
  smoke-inspector debt outside this product diff.
- Two identical builds: 358 files / 26,237,792 bytes / manifest
  `94fe23488b99eb1b30d896a1346f80def24897b782f8f566e1551b18448c736f`.
- Immutable production: 361 files / 26,247,963 bytes; three exact retained
  files; full manifest `40ea966b...4dda`; owner/mode/path/symlink drift zero.
- Production Chromium at 390x844 and 568x514 verified both unclear and price
  outcomes with zero overflow, console error, POST, storage or URL mutation.
- External and VPS GET-only smoke passed 14/14 after activation, during the
  real REL-0172 rollback and after forward restoration.
- Final bot PID is `557663`, `NRestarts=0`; Nginx is valid, SQLite is WAL with
  `quick_check=ok`, and the release-window journal has zero actual lock,
  traceback, ERROR or CRITICAL matches.

## Rollback

Static rollback is immutable `release172-df007e4`; it was executed and then
forward-restored. No backend or database rollback belongs to REL-0173 because
neither changed. Production backup:
`/root/site-backups/release173-retention-preview-20260825T133327Z`.

Exact chronology, hashes, browser metrics and safety boundaries are in
[`E-1037`](../evidence/E-1037.md). Publication proves presentation and release
mechanics, not conversion, revenue or profit uplift.

# REL-0160 — понятный объём отчёта по практике

- Date: 23 August 2026, Europe/Moscow.
- Canonical source: `135cb4559ee6c81b0e42633d2ebb2ad48abbb1a1`.
- Product result: `89a2887eff51561e519c1357e5da88046b9c9d9c`.
- Workstream: `WS-5948fc36e7634bc2936a1d53398d4216`.
- Production path: `release160-135cb45`.
- Published after forward restore: `2026-08-22T23:35:13Z`.
- Static rollback: `release159-57703fa`.
- Backend: unchanged Analytics v2 contract 2.3.0.
- Verdict: **GO**. G10 has P0=0/P1=0 after exact source/build/hash proof,
  live browser, two-vantage smoke and an executed static rollback/forward.

## Outcome

The public practice-report page now lets a visitor select a standalone written
diagnostic from 2 500 RUB, editing of a ready factual package from 8 000 RUB, or
staged support from 14 000 RUB before contact. One selection owns both later
continuations and arrives in the configurator with the same result and starting
situation.

The page explains why 14 000 RUB is a different linked-document scope, names
the factors that may increase a lower orientation, and separates a preliminary
range from the fixed stage price in the prepayment specification. Existing
stage stop, authorship and compatible diagnostic-credit boundaries remain
visible. The Otisk composition, light/dark themes and responsive system were
preserved; there is no discount, fake scarcity, grade promise, fabricated case
or uplift claim.

## Release proof

- Site 584/584; backend 31/31; Brain 39/39; focused 32/32; strict validation,
  diff checks and two deterministic builds green.
- Public payload: 353 files / 25 006 595 bytes, digest `4bba587f…efcdf`.
- Immutable tree: 356 files / 25 016 766 bytes, manifest `4b6c07fb…c2e9b`;
  source parity 353/353 and owner/mode drift 0.
- Exactly two public files differ from release159; repository/tree/HTTP hashes
  agree for both.
- Production Chromium at 390/1440 light/dark: overflow 0, console 0; support
  route and three structured minimum prices verified.
- External and VPS smoke passed 14/14 after activation, 14/14 in the executed
  release159 rollback and 14/14 after final forward restore.
- Backend, database, Analytics, admin and submission paths were unchanged.

Exact hashes, browser/runtime observations and rollback chronology are in
`E-1021`.

## Live exact result and rollback

`current` and compatibility `dist` resolve to `release160-135cb45`;
`previous` resolves to `release159-57703fa`. Service is active, Nginx syntax is
valid and SQLite is `ok`. Static rollback is the paired pointer switch to
release159; no backend restore or restart is part of this release.

## Residual boundary

The acquisition source of the lost lead is unknown because admin and analytics
were intentionally outside scope. Release correctness does not prove business
uplift. Measure it only after a sufficient organic consented sample and a fixed
comparison window.

# REL-0100 — service-choice release

- Source commit: `1c275bffde9368d52433a137bca11666b7d1230c`
- Production path: `release100-1c275bffde93`
- Published: 3 August 2026
- Rollback: `release99-96156040130c`
- Verdict: GO in the verified read-only production contour.

## Scope

The release is the deterministic public build of the exact canonical source. It
publishes the integrated `OUT-005` result: the services catalogue preserves the
client's explicit or saved intent, exposes one contextual continuation, carries
allowlisted detail-page routes into the configurator and keeps exact discipline
pricing local until the established API transport boundary.

No production submit, OAuth mutation, payment, upload, client-data access or
deletion was part of the release proof.

## Pre-publish gates

- Exact canonical source: `1c275bffde9368d52433a137bca11666b7d1230c`;
  frozen OUT-005 result: `db93a45a385521600fbe1a5121334c413ebdbfa4`.
- Product regression: 482/482 pass, 0 fail, 0 skipped.
- Brain suite: 39/39 pass; `brain validate --strict`, changed JavaScript syntax,
  catalogue diff checks and `git diff --check` pass.
- Deterministic public build: 339 files, no Brain/test/internal tree; SHA-256 tree
  digest `d503a87c7431c0760adc28bd0ffdc7a0b46592147eb24688d02a56723df309cf`.
- Pre-release server backup:
  `pre-release100-1c275bffde93-20260803T013955Z.tar.gz`, 19,227,593 bytes,
  SHA-256 `4dbbde4bdf7e316be055a4b87f4804b9131463507d96a2441dd93c532555f971`.

## Production evidence

- Nginx `current` and compatibility `dist` both resolve to
  `release100-1c275bffde93`.
- HTTP 200: home, services, configurator, order, cabinet, privacy and admin; a
  definitely missing route returns 404.
- Live SHA-256 values match the staged release: `assets/js/app.js`
  `2e7a955072d6ae595dbd7d5c5341e20f6e33427e5c226f2ac9dd99ad45cc7be8`,
  catalogue JavaScript
  `48ed111f36b871f81ad0d890f75bd095294ed161a4db84539992a6251c4dbf3b` and
  catalogue CSS
  `2d0280db4b768c9484d12be5466548a2df3affe1fa9415858e3a87008ee847b0`.
- The checked-in read-only production smoke passes 14/14 from the server with
  its default runner and 14/14 externally with bounded transport retries.
- Independent browser proof returned P0=0 and P1=0. At 390×844 and 1024×900,
  fresh, selected and saved service states show the intended single primary;
  light/dark geometry has no horizontal overflow and the console has zero
  errors or warnings. The psychology route preserves `disc=psychology` and
  reproduces the exact visible quote 11,000–15,500 ₽.

## Rollback proof

Rollback was executed, not inferred. Both symlinks were atomically switched to
`release99-96156040130c`; home and services returned HTTP 200 and the public app
hash became
`70ea63dd8fbf55ddb50eb5677aabb811f6ddf67b626c744f00917e564de0db0b`.
They were then atomically restored to release100; home, services and configurator
returned HTTP 200, both resolved targets matched, and the public app hash returned
to `2e7a955072d6ae595dbd7d5c5341e20f6e33427e5c226f2ac9dd99ad45cc7be8`.

## Recovered release observations

The first switch exposed HTTP 403/404 because the newly created inactive release
directory inherited mode 700. The hard gate triggered an immediate rollback to
release99 before acceptance. Ownership/modes were corrected on the inactive
release (`www-data:www-data`, directories 755, files 644), its hashes remained
unchanged, and all later gates passed.

Some local HTTPS requests timed out intermittently. A transient 13/14 result was
also reproduced against unchanged release99, while the checked-in server-side
runner and the final bounded external runner each passed 14/14 on release100.
Treat recurrence as an operations/network observation, not an application
regression; exact final hashes and the independent browser proof are green.

## Residual risk and next outcome

`OUT-001` remains blocked from production execution until authoritative backend/
bot evidence and a safe marker, lookup and cleanup procedure exist. The next
safe product slice is `OUT-006`: measure first-step comprehension and time to
action without sensitive data, then use that evidence to justify any further
catalogue or journey redesign.

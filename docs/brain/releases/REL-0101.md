# REL-0101 — analytics privacy foundation release

- Source commit: `b9837a34c4e39e0adfb725dcf5f76f4e96f1e30e`
- Production path: `release101-b9837a34c4e`
- Published: 3 August 2026
- Rollback: `release100-1c275bffde93`
- Verdict: GO in the verified read-only production contour.

## Scope

The release is the deterministic public build of exact integrated canonical. It
publishes the invisible OUT-006 frontend privacy foundation: analytics requests
cannot carry cabinet credentials, private/demo contours are silent, path and
event dimensions are finite, legacy attribution is canonicalized, revoke and
expiry purge browser state, and question contact data is no longer linked to the
analytics browser ID.

No new `first_step_*` milestone, visual redesign, production submit, OAuth,
payment, upload, analytics-consent mutation, client-data access or deletion was
part of the release or its proof.

## Pre-publish gates

- Exact canonical source: `b9837a34c4e39e0adfb725dcf5f76f4e96f1e30e`;
  verified privacy implementation: `1011060c9b2f30b55809ab9bb253ae64cf811925`.
- Failure-first contract 1/5→10/10; independent final P0=0/P1=0/P2=0 and
  59/59 focused checks.
- Product regression: 492/492 pass, 0 fail, 0 skipped. Brain suite: 39/39;
  strict validation, JavaScript syntax, generated-bundle parity and diff checks
  pass.
- Deterministic public build: 339 files; SHA-256 tree digest
  `66c4859c80de8523d9d5eee702239b163d9e7e04f6cdc2709e42487a1767e722`.
  Rebuilding release100 through the same selection produced its recorded digest
  `d503a87c7431c0760adc28bd0ffdc7a0b46592147eb24688d02a56723df309cf`.
- Pre-release server backup:
  `pre-release101-b9837a34c4e-20260803T031506Z.tar.gz`, 19,234,916 bytes,
  SHA-256 `82a92310e196ef52da78c53aa34aacab1d5b0d4673a9f09e8e69b391db03fb1b`.

## Production evidence

- Nginx `current` and compatibility `dist` both resolve to
  `release101-b9837a34c4e`; the release has 339 files, `www-data` ownership,
  directory mode 755 and file mode 644.
- HTTP 200: home, services, configurator, order, cabinet, privacy and admin; a
  definitely missing route returns 404.
- Staged and live SHA-256 match: `assets/js/app.js`
  `c46d3984aa291b611af16f2fea808e15a92f7178df18591ee9a5ad8eda66ec41`,
  home bundle
  `d914b8755999e841b90a49474ae39b43d5bc7815b70162183ea8384226c5b357`
  and supporting runtime
  `cf6f54cca5241436aef2684012cd3ef0f837d6337344b532c49e720db4489381`.
- The checked-in GET/HEAD-only production smoke passes 14/14 externally and
  14/14 when executed on the VPS. Consent document truth matches the release.
- An isolated production WebKit session at 390×844 and 1024×900 had no
  horizontal overflow and zero console errors/warnings. Fresh no-consent home
  and services sent no `/api/visit`; the expected privacy cache wave was live;
  an arbitrary 404 path evaluated to `/other`. The browser was closed.

## Rollback proof

Rollback was executed. Both symlinks were switched to
`release100-1c275bffde93`; home and services returned HTTP 200 and the app hash
became `2e7a955072d6ae595dbd7d5c5341e20f6e33427e5c226f2ac9dd99ad45cc7be8`.
Both were then restored to release101; home, services and configurator returned
HTTP 200 and the app hash returned to
`c46d3984aa291b611af16f2fea808e15a92f7178df18591ee9a5ad8eda66ec41`.

## Operations observations

Several SSH banner attempts timed out before a session existed; pre-switch
failures left release100 and its symlinks unchanged. The first external smoke
also saw one transient `/plans` transport failure; the bounded final external
run and the VPS run each passed 14/14 against unchanged release101. No failed
attempt reached a product mutation.

## Residual risk and next slice

The repository still does not prove `/api/visit` server IP handling, dedupe,
idempotency, retention or aggregate readback, so new comprehension milestones
remain blocked. User feedback and independent runtime audits now reproduce P1
search presentation and catalogue findability problems. They belong to a fresh
scoped product workstream with protected OUT-005 route/price/schema contracts,
not to this privacy release.

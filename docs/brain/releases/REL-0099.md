# REL-0099 — integrated contract release

- Source commit: `96156040130c874519d1cd4f6335173a774d7847`
- Production path: `release99-96156040130c`
- Published: 3 August 2026
- Rollback: `release98-c30dbd4924b5`
- Verdict: GO in the verified read-only production contour.

## Scope

The release is the exact public build of the current canonical source. It carries
the integrated post-release98 contracts for submit safety (`OUT-001` local
contract only), cabinet priority/context (`OUT-002`, `OUT-004`) and the shared
shell (`OUT-003`). It does not claim the still-unexecuted downstream production
submit/delivery proof.

The shared shell changes cover saved-theme bootstrap parity, consent settings
and modal isolation, removal of the conflicting admin shortcut, corrected dark
CTA/badge contrast and the atomic `20260803out003shell1` asset key across all 90
managed consumers.

## Pre-publish gates

- Exact detached source: `96156040130c874519d1cd4f6335173a774d7847`.
- Product regression: 472/472 pass, 0 fail, 0 skipped.
- Brain suite: 39/39 pass; `brain validate --strict` valid; `git diff --check`
  and changed JS syntax checks pass.
- Deterministic public build: 339 files, no Brain/test/internal tree; SHA-256 tree
  digest `dad9e1476332644993cd3a6992bf036ee92712fb527c58bfbc1e01fe5eee59b6`.
- Pre-release server backup:
  `pre-release99-96156040130c-20260802T235858Z.tar.gz`, 19,244,187 bytes.

## Production evidence

- Nginx `current` and compatibility `dist` both resolve to
  `release99-96156040130c`.
- HTTP 200: home, services, configurator, order, cabinet, privacy and admin;
  a definitely missing route returns 404.
- Live `assets/js/app.js` SHA-256 is
  `70ea63dd8fbf55ddb50eb5677aabb811f6ddf67b626c744f00917e564de0db0b`;
  the live home carries `20260803out003shell1`.
- Checked-in production smoke: 14/14 pass using GET/HEAD only. Public catalogs,
  unauthorized 401/403 boundaries, events and the deployed consent version all
  match the source.
- Browser smoke: 390 px mobile and 1440 px desktop; direct service and cabinet/
  admin routes retain dark theme, show zero horizontal overflow and emit no
  console warnings/errors. Mobile menu opens with the expected accessible state.
- No order submit, OAuth mutation, payment, client-data access, upload or data
  deletion occurred.

## Rollback proof

Rollback was executed, not inferred. Both symlinks were atomically switched to
`release98-c30dbd4924b5`; the old live app hash became
`e07c8d0c16f9e20e9ac86e01419ee24dc1bee11044b13f763f56be38c06440b0`.
They were then atomically restored to release99, whose internal and external app
hash returned to `70ea63dd8fbf55ddb50eb5677aabb811f6ddf67b626c744f00917e564de0db0b`.

## Residual risk

Several new SSH/HTTPS handshakes from the release runner timed out during
preflight. No partial switch occurred, retries recovered, final HTTP/API/browser
checks were green and the final external hash matched. Treat recurrence as an
operations/network observation; it is not evidence of an application defect.

`UXD-0001` remains open: the real frontend → API → bot/operator → cabinet path
still requires a safe marker, lookup and cleanup proof before one production
request can be authorized.

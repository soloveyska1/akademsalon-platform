# Workstream handoff

- Branch: codex/salon-form-polish-20260924; exact fresh canonical base 5e1cb620c1f670cd90a2015e458a2027e4a381cf. Outcome OUT-006, sole writer root. User asks to continue small comfort/beauty features after release219.
- Scope: own scripts/evidence trees and generated handoff. Bounded static overlay on live release219. No backend, contacts, prices, payload schema or consent contract changes. Original user-dirty .claude/launch.json excluded.
- Before observations: contact is one autocomplete-off generic text input; client validation message is below consent far from the focused field; aria-invalid remains after correction until next submit. Native maxlength is present, but topic has no counter and details counter does not show the limit.
- Goal: local helpful error text next to affected fields, soft blur/correction behavior, optional channel hints for keyboard/placeholder (Telegram/email/phone/VK) without rewriting contact; readable near-limit counters for topic/details.
- Acceptance: preserve authoritative Salon.valid.contact semantics and original submit ordering/one validation_error event; no error on pristine empty field; local invalid field error linked through aria-describedby, cleared as corrected; native required service/consent controls reveal existing disclosure and focus usable custom select trigger; frozen/retry state cannot mutate via new controls; channel selection alone is not first_input and never alters the value or payload; actual input still counts once. Hints only, never claim deliverability or contact verified. No text/contact in persistence, telemetry or remote lookup. Counter uses native UTF-16 maxlength units, handles paste/prefill and only announces threshold changes, never every keystroke. No focus stealing on blur/input, reduced motion, 44px channel buttons, 360..1440 both themes.
- Design: existing Golos Text/Literata, purple accent/soft sheet/ink/line tokens. Signature: slim understated channel tabs with one accent underline, not a new large card. Inline errors small with text + icon/line to avoid color-only status; counters quiet except nearing limit. No hero/layout redesign or added mandatory step. Retain one primary CTA. This reflects established Salon rather than generic form chrome.
- Proof before edits: exact live baseline inventory, failing-before browser observations, hermetic contact/blur/submit/native validation/chip/lock/paste/counter/keyboard/privacy tests plus prior comfort/funnel suites; full deterministic tests and strict Brain; two independent reviews. Hash-identical tested runtime then actual apply/rollback/forward and live GET-only smoke.
- Risks/rollback: live root differs from source root; never rebuild stale canonical HTML. Overlay only on expected current release219, preserve compatibility dist203. Rollback static current pointer, no database restore.
- Changed: declaration only. Unverified: new runtime and release.
- Next: commit declaration and strict conflict review; build prototype and obtain reviews.

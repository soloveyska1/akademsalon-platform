# Admin replacement

Outcome: OUT-006. Owner: codex-root. Base: e53d11cea40055f2dae41a0f212cbbac4c79055f.

User explicitly rejected the previous admin shell as cosmetic. Replace the entire frontend entry and all module markup/styles/controllers. Preserve existing backend contracts and real data. No legacy admin renderer or stylesheet imports. Shared auth/API infrastructure remains.

Scope union exists to integrate previously published release201 history; substantive changes bounded to admin.html, new salon-admin-workspace CSS/JS and salon-admin-core, new tests and own evidence/handoff. No cabinet, public offers, economics, backend mutations or unrelated design changes.

Design: an operator's desk with ink-purple navigation, white/lavender surfaces, Manrope body, restrained Literata headings, tabular money. Master/detail order workspace; priority inbox instead of decorative metric wall. All 11 operational modules freshly rendered, finance and delivery actions carry exact order context. Light/dark and 320–1440 responsive. Signature: a compact live work queue beside the selected case, with visible next action and a separate change confirmation.

Ready criteria: no legacy UI imports; inventory-to-implementation module parity; real handler tests for identity, duplicate/unknown mutations and validation; synthetic browser journeys across all modules, keyboard/mobile/dark; two independent read-only reviews; required regression suite. Publication only after P0/P1=0, immutable hash parity, smoke and tested rollback. No real client mutations in QA.

Current: manifest bootstrap, no implementation yet. Next: strict conflict check, fresh fetch, integrate submitted release201 branch then resolve scope checks before source edits.

# Workstream handoff

- Branch: `claude/mobile-shell`
- Outcomes: `OUT-008`
- Goal: этап 1 плана `OUT-008` — оболочка приложения на телефоне. Владелец
  выбрал этот этап первым 2026-08-06 из четырёх предложенных. Закрываются три
  измеренных в `E-1014` разрыва: кабинет остаётся без нижней навигации,
  конфигуратор и `start.html` не дают выхода в остальной сайт, а баннер
  согласия занимает нижние 200 px и прячет док при первом визите.
- Acceptance: на 390×844 у кабинета есть постоянная нижняя навигация; с экрана
  конфигуратора есть именованный выход в сайт; баннер согласия не перекрывает
  поле ввода и не отнимает нижние 200 px; тач-цели новой оболочки ≥44×44;
  `node --test tests/*.test.js` зелёный целиком; десктоп 1440 не меняется.
- Proof: `E-1014` как база до правки; failing-first `tests/mobile-shell.test.js`;
  повторный замер тем же харнессом WebKit 390×844 после правки; сравнение
  десктопного снимка до и после.
- Changed: пока только manifest и этот handoff.
- Unverified: реализация не начата. Отдельно не проверено поведение кабинета в
  залогиненном состоянии — `.account-nav` создаётся только при активной сессии,
  а замер шёл анонимно.
- Risks/rollback: правки затрагивают общий shell (`chrome.css`, `mobile.css`,
  `app.js`) — риск задеть десктоп и главную. Главная живёт на сборке
  `home-release.min.*`, поэтому после любой правки исходников обязателен
  `node scripts/build-home-release.mjs`. Откат — удаление ветки; в прод ничего
  не публикуется без отдельного бампа кэш-ключей семьи shell и решения
  владельца.
- Next: написать падающий тест на три разрыва, затем править оболочку.

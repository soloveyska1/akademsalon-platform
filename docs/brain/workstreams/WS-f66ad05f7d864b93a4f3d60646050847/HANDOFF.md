# Workstream handoff

- Branch: `codex/out-006-practice-support-continuity-v4`
- Outcomes: `OUT-006`
- Goal: preserve the public `practice + draft + support` supplied-material
  scope through the configurator, cart, request payload and prepayment
  specification without lowering its 14,000–19,500 RUB `vip` quote or changing
  the Otisk visual concept.
- Acceptance: the exact route is explicit A1 everywhere, names the practice
  package and requirements-map first result, accepts a file or clear
  40-character description, shows its transferred scope before compact-screen
  fields, and links scope changes to the public 2,500 / 8,000 / 14,000 selector.
  Cart, request and specification remain supplied-material editing; explicit
  A1/A2 outrank legacy tariff inference, while absent-mode `vip` remains A2.
  The single admin runtime URL carries a bounded new cache key so the resolver
  reaches the specification screen. One primary action and all existing legal
  and price boundaries remain.
- Proof: failure-first cart/request/specification contracts; full public,
  backend and Brain suites; strict validation, diff check, two deterministic
  builds; Chromium exact/generic routes at 390/1024/1440 light/dark; three
  independent post-implementation reviews; protected preview and production
  smoke plus executed static rollback/forward.
- Changed: none yet.
- Unverified: implementation not started.
- Risks/rollback: making every `vip` row A1 would weaken the lawful generic A2
  path; letting the tariff override an explicit mode would recreate a
  bait-and-switch; omitting cache delivery would leave the fix browser-stale.
  Explicit modes win, legacy inference remains only for missing metadata, and
  the cache key changes without touching admin UI. Roll back the bounded result
  if any surface disagrees.
- Next: commit the declaration, run strict conflicts, carry forward the two
  already verified implementations, add the one cache parameter and repeat all
  release gates from the exact canonical base.

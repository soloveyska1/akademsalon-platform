# Workstream handoff

- Branch: `codex/release103-search-trigger-v3`
- Outcomes: `OUT-007`
- Goal: replay the already verified release103 search-trigger breakpoint closure
  on exact fresh canonical `cd035ac7f7739345fff7a11236ede679e8336ba0`
  without changing search semantics, catalogue IA or protected OUT-005
  contracts.
- Why v3: v2 correctly abandoned after Brain rejected an attempted canonical
  merge as stale-base/scope escape. This workstream was generated by
  `brain workstream init` from fresh canonical before any product replay; its
  scope was reconstructed through the init CLI, not by copying a manifest.
- Reproduced failure: live release102 and rollback release101 expose zero
  visible pointer search triggers on home, services and dashboard at 921–1240
  px. Keyboard shortcuts are not a discoverable substitute, so G5/G6 classify
  the reachability gap P1.
- Implementation boundary: replay failure-first `ed999a5`, bounded result
  `0d32cd2` and cache/seam closure `e97a66a`; then recreate the durable E-1010
  proof and v3 handoff. One global shared-CSS rescue covers both header variants
  from 920 through 1240 px, with a deterministic home CSS rebuild and CSS-only
  cache marker on 89 direct consumers plus the home bundle.
- Acceptance: exactly one named 44×44 trigger across the boundary; center hit,
  no overlap/overflow; open/focus/query/Escape return; no error/unsafe request;
  89/89 cache consumers; unchanged JS/search/catalog semantics; every changed
  path declared; P0=0/P1=0.
- Proof: focused 56/56, full 501/501, Brain 39/39, strict validation,
  deterministic double build, byte/hash closure, WebKit 42/42 and Chromium
  24/24 with forced scrollbar. Three independent Codex reviews plus daily
  Kimi/Sonnet/GLM and one Opus all approved the exact content to replay.
- Stop conditions: undeclared path, semantic drift, zero/multiple trigger,
  dead exposed control, layout overlap, generated/product JS drift, protected
  inventory drift, failed gate or any P0/P1.
- Rollback: revert the narrow CSS/cache result. Production remains release102;
  deployment is prohibited in this product workstream and belongs to a fresh
  `production:deploy` workstream with immutable release, automatic rollback,
  two-vantage smoke and executed release102 rollback/forward.
- Changed: declaration only; no product replay yet.
- Next: commit exactly manifest plus handoff, run strict conflicts, then replay
  the three bounded commits and reconstruct verified durable evidence.

# E-1023 — Release162: проверяемый состав сопровождения до контакта

- Date: 23 August 2026, Europe/Moscow.
- source_type: exact canonical build + deterministic conditional-visibility
  contracts + immutable static release + two-vantage GET/HEAD-only smoke +
  real Chromium + executed static rollback/forward.
- Canonical production source: `977c1f6a52a38c59ea5bbfc9c93910b8e615495c`.
- Verified product result: `29376844dc9691f0f9f8f40dbd83ebcdc0f2d1ee`.
- Workstreams: `WS-5b52b008e728481aa5b012c1299fc143` and
  `WS-22c20caf674f4e478575eb974368db42`.
- Outcome: `OUT-006`; release gate: `G10`.
- Production release: `release162-977c1f6`.

## Пользовательский результат

The existing 2,500 / 8,000 / 14,000 RUB selector remains the single pricing
mechanism. Only the selected 14,000 RUB staged-support row expands a result
passport. Before contact it makes five contract dimensions visible:

- required factual inputs: program/methodical guidance, existing report and
  diary drafts, real dates/tasks and applications;
- four outputs: requirements/missing-items map, stage plan, agreed editor
  versions and final completeness/signatures/applications checklist;
- authorship: the student supplies and confirms facts, dates and completed
  tasks;
- exclusions: no independent fact collection, substitute attestation work,
  fictional diary, grade/admission/acceptance/commission guarantee;
- commercial boundary: 14,000 RUB is the lower orientation for the whole
  listed composition; total and stage split are fixed before payment, and new
  out-of-scope work is quoted separately.

The specification link is secondary and explicitly says its data are
fictional. Diagnostic and editing do not inherit the passport. The already
verified release161 route/request/specification contracts remain byte-for-byte
unchanged.

## Source and deterministic proof

On exact canonical product source:

- public product 590/590;
- backend 31/31;
- Brain 39/39;
- strict Brain validation and `git diff --check` green;
- three independent final reviews: P0=0, P1=0, P2=0.

Two `dist/client` builds are byte-identical: 353 files / 25,039,823 bytes,
deterministic digest
`c7a7fffcc84ed8c984225b9cb72ec030423615351be2eb5f9c5a7c6f2c34dd7c`.
Owner-only protected Sites preview version 38 was saved and deployed from the
same canonical source before public activation.

## Production preflight and immutable payload

Before static mutation:

- `current=dist=release161-1c5f0ee`;
- `previous=release160-135cb45`;
- service active, Nginx valid, SQLite `ok`;
- external and VPS read-only smoke 14/14 each;
- target and staging release162 paths absent.

The inactive release was assembled from immutable release161 and the exact
public-client build without deleting server-owned assets. Exactly two public
files changed:

| Public file | release161 SHA-256 | release162 SHA-256 |
| --- | --- | --- |
| `otchet-po-praktike.html` | `518df2272137840182075cf6a933bc0048e9b72b113efb3ea7138a7c7bec6a05` | `7d445798eac5f4d7f75579805b614ce6eb506c1034ce1ae87302667442bdcd80` |
| `assets/css/polish15-catalog.css` | `a1109bda0566f3d3a484f18ad3a136e187c3912f657f8c6924c632c76fb268e4` | `b5a9c2e2f7428d5bc3a2e65ab9a36b9401a71f096aec3b979b7392a8f1bf7b83` |

Repository, `dist/client`, inactive release and live HTTP hashes agree for both
release162 values. The two changed files have distinct inodes from release161;
release161 hashes remained unchanged.

The server-owned files were preserved exactly:

- `.indexnow-key` —
  `f3930289da0464745613a2dd2d345d24cd78c60e665f775c2c07320d4bad8af8`;
- `analysis/growth-baseline-2026-07-24.ipynb` —
  `0929da3eb83ab650bd742639a61beaf2828ed81420d95bc7806c0d1ff8deb039`;
- `sw.js` —
  `f500c4f386717e39ff85dbf40ec7dac1a855b1044321081e05e67f2787275648`.

Inactive and final release162 have 356 files / 25,049,994 bytes, full streamed
manifest digest
`20cd81dc27c97241cf32b4589cf77bf5d4a4f0275deb21f6191989844fa64a70`,
353/353 source parity, owner/mode drift 0 and no unexpected public addition or
deletion.

## Live browser and route proof

Release162 first activated at `2026-08-23T02:48:47Z`. Exact HTTP SHA-256 for
both files matched the repository and immutable tree.

Real production Chromium proved:

- default editing at 390 px: passport hidden, `от 8 000 ₽`, two exact editing
  routes and root overflow 0;
- support at 390 px: passport visible, `от 14 000 ₽`, two exact support routes,
  one primary in the pricing screen, 54 px disclosure target and root overflow
  0;
- native-radio Arrow keys return to editing and hide the passport while keeping
  focus on the selected control;
- support → fictional specification → Browser Back restores support, 14,000
  RUB status and both support routes rather than resetting to editing;
- 1440 px keeps four outputs in two columns; 390/1440 light/dark have root
  overflow 0, and the dark output numbers retain readable contrast;
- support continuation reaches `Сопровождение комплекта по практике`, the
  exact requirements/stage-plan first result and 14,000–19,500 RUB range;
- final post-forward reload retained support state and desktop columns;
  console errors/warnings are 0.

The production browser accepted only necessary cookies. No form was submitted
and no client or analytics record was created.

## Executed rollback and final state

After activation, external and VPS smoke passed 14/14. At
`2026-08-23T02:54:09Z`, both static pointers were moved to release161 and
`previous` to release160. The old release161 HTTP hashes returned, while
external and VPS smoke again passed 14/14.

At `2026-08-23T02:55:00Z`, forward restore moved `current` and `dist` to
release162 and `previous` to release161. Final external and VPS smoke passed
14/14.

Final production truth:

- `current=dist=release162-977c1f6`;
- `previous=release161-1c5f0ee`;
- service active, Nginx valid, SQLite `ok`;
- immutable files 356 / bytes 25,049,994 / manifest `20cd81dc…a70`;
- repository and live HTTP hashes agree for both public changes;
- temporary pointer residue 0.

Backend and infrastructure hashes stayed byte-identical:

- `webapp.py` —
  `6c30cf2f02fcda1247131cc87078bb643307b5746880825bf7b6bec4fce55c05`;
- `db.py` —
  `7611e08b69bbe283b5614fbaaf5bd3f379b95cefb57dcc033b23446ca051dad9`;
- `analytics_v2.py` —
  `69a9d642263a88570abadc7f56c06138834c573d6f0049b1d7d02b8bc28991ea`;
- `analytics_contract_v2.json` —
  `e05bfd4b5b7588f35815ef32de06760e3ec2400b08e6790a9d6c268058021f99`;
- active Nginx site —
  `e66545c320c7ac2c2f84f76a3d4b9d87c216179fdd67388e573ea3e1d489003d`;
- security headers —
  `ebe20503129e3b856f9e3d645ee206877dcc067d21fe7f6b2597c4b2b8e6d77d`.

The backend service was not restarted or modified.

## Privacy and inference boundary

No production contact, order, client file, message, payment, raw analytics row,
IP/UA, query/referrer or OAuth material was read or changed. Public checks used
GET/HEAD and ordinary public navigation only; system checks used hashes,
service state, Nginx syntax and read-only SQLite integrity.

This evidence proves release correctness and offer continuity, not conversion
uplift. The rejected lead's source remains unknown and no practice-specific
review or outcome was invented.

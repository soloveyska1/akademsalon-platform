# E-1029 — production first-order campaign and rollback proof

- Date: 24 August 2026, Europe/Moscow.
- source_type: exact backend installer readback + immutable static manifests +
  two-vantage GET-only smoke + production Chromium journeys + executed
  backend/Salon/Kladovaya rollback and forward restore.
- Canonical Salon source:
  `afe47556c9dcdeda3c69d20601ab11fc806fb399`.
- Verified product result:
  `157d6f7a0507bb091d313acce7412c751ca9bce9` (`E-1028`).
- Canonical Kladovaya source:
  `ff8484087d841aaa4cb1c7deea1fe8dd90eb791f`.
- Release workstream: `WS-8bbb978ad7db43cf8d92fbd764bad75b`;
  release `REL-0168`; outcome `OUT-006`.
- Final readback: `2026-08-24T09:51:59Z`.

## Backend-first activation

The pinned installer was checked against the real production database at
`/root/salon_bot/salon.db` before mutation. Its exact source transition was:

- `app/webapp.py`: `14a45362e9ce17416c552557f4610546ce23549a4fc35dbbccc46d9e624448ee`
  -> `48a1f40a5bf49f88de3e079e3576075d8b08d5e561ad6d7ffdfdbf1d0d67008b`;
- `app/db.py`: `7611e08b69bbe283b5614fbaaf5bd3f379b95cefb57dcc033b23446ca051dad9`
  -> `be6bf8c89cb4024590e41f1b698b2cb4839d626648079cfcc0274dd23c1a8899`;
- `app/services/promo.py`:
  `6f6047feb78cca469655e8dbd023c09ca1c861c7ec53c3675c0cac9870dd6db9`
  -> `f912bec36e1c1cdafae9d5e449919c5414215a782ac2ecc6cbebead74ddd4c21`.

The first apply created
`/root/salon_bot/backups/first-order-promo-20260824T093953211664Z`.
Python compile, service startup, Nginx and SQLite quick-check passed. The
campaign setting was `on`, all three additive tables existed, Kladovaya status
was `active` and a clean Salon request was `provisional`.

The backend rollback was executed, not simulated. It restored all three exact
before-hashes, kept the live SQLite database and additive tables, set the
campaign to `off`, restarted the service and made the uninstalled route fail
closed (`405`). SQLite stayed `ok`. Forward apply then created the final
rollback copy
`/root/salon_bot/backups/first-order-promo-20260824T094143186111Z`, restored
the exact post-hashes, setting `on`, active status, service and SQLite health.
Final installer `--check` reports `changed=false`, `database_current=true` and
`source_state=installed`.

The first health loop used an obsolete unrelated port (`4310`) and therefore
reported connection refusals while the service was already healthy. Read-only
status and journal inspection identified the configured listener `8090`; all
subsequent checks used that real port. This was an observation error, not a
service failure.

## Academic Salon immutable release

The canonical build reproduced the reviewed public payload exactly: 356 files,
26,170,243 bytes and digest
`dfc48a71d942acf4a2cd5e0a068447f39eebc5303a2cef0cb2b110733f598a64`.
The immutable tree `release168-afe4755` adds the three exact server-owned files
and contains 359 files / 26,180,414 bytes, full-manifest digest
`0da6d7477690f31b513d2c40f8bc7330ae0a30fdb645bd4fa63dbab6d1b35d81`.

Server-owned hashes stayed exact:

- `.indexnow-key`: `f3930289da0464745613a2dd2d345d24cd78c60e665f775c2c07320d4bad8af8`;
- `sw.js`: `f500c4f386717e39ff85dbf40ec7dac1a855b1044321081e05e67f2787275648`;
- `analysis/growth-baseline-2026-07-24.ipynb`:
  `0929da3eb83ab650bd742639a61beaf2828ed81420d95bc7806c0d1ff8deb039`.

Repository, immutable tree and live HTTP agree on the campaign-bearing hashes:

- `index.html`: `49d59fb3df5bd0982e9ac49b20d3ddf52c6e2525fdd8ba806cf698418988bda4`;
- `configurator.html`:
  `a5b1da22b634f716cdede311d9f2bb9379595c3fa18260d374e4823a099d1ef4`;
- `assets/js/promo-campaign.js`:
  `e5a1cf58bf1cbfb0cd86b630a2dd57cff38658523305096ee61f8fd13bf7596a`;
- `assets/css/promo-campaign.css`:
  `3eedde6b28a4ed00ec630687db7c3c382dea598cc6585a12d3f60f6b703244ad`;
- `assets/img/promo-salon-welcome.png`:
  `ade0d184e16ab234fbc08923159930dd41d92bf229dc75b1e2e0578eb769236e`.

Activation, rollback to `release166-a9e038e`/`release165-5b1735c` and forward
restore to release168/release166 were all atomic. External and VPS smoke passed
14/14 after initial activation, during rollback and after forward restore.
Service stayed active, Nginx stayed valid, SQLite stayed `ok`, and temporary
pointer residue was zero. The static pointer backup is
`/root/site-backups/release168-first-order-promo-20260824T094324Z`.

Final Salon pointers:

- `current` and compatibility `dist` -> `release168-afe4755`;
- `previous` -> `release166-a9e038e`.

## Kladovaya immutable release

The exact canonical build contains 554 `dist` files / 50,030,879 bytes. The
complete immutable tree
`20260824T0947Z-first-order-promo-ff84840` contains 559 files /
50,051,737 bytes with full-manifest digest
`c65ae6a7944f6f65a4442cd17e5f5bb6447c3caecb1d50947a7875a97e22bf92`.
Local and remote hashes agree on:

- `dist/server/index.js`:
  `00ea567b9e17b7583809663704a310f034dacf57d04018eee4d03537972c2920`;
- `dist/client/images/promo-first-sheet.png`:
  `05f1431f98bf6d8d5d50e131a0ec2e6422069bccbb5537adb2b05c9105c74993`;
- `node-server.mjs`:
  `566881eb0a91a0a0362c32d84447895d6bfe5ffa8bfcafc1d1e5245ccc5ce627`.

The site was activated, rolled back to
`20260823T0645Z-growth-lecture-seo` and restored forward. During rollback the
home remained `200`, redirects remained correct and `/promo-status` returned
`404`; after restore it returned `200 active`. Service and Nginx stayed green,
and pointer residue was zero. The pointer backup is
`/root/site-backups/kladovaya-first-order-promo-20260824T094734Z`.

## Production interaction proof and boundary

Real Chromium covered these read-only states after final forward restore:

- clean Kladovaya at 390x844 and 360x800: the branded sheet, exact code,
  paid-site boundary and one outbound CTA were visible; same-origin status was
  called, body scroll locked, horizontal overflow was zero and console was
  empty;
- a durable `kladovaya:*` marker: zero dialog and zero status call;
- Kladovaya `?offer_preview=welcome`: labelled preview, zero outbound link and
  no durable owner/seen marker;
- clean Salon at 390x844: one visible branded welcome dialog, exact code,
  inert background, locked body, zero horizontal overflow and empty console.

Final external responses are `200` for both homes and both promo-status/
eligibility routes. Kladovaya's final status body is exactly the allowlisted
`active` campaign state. No form was submitted; no production order, client
contact, file, payment, message, analytics event or retention grant was created
or read. Owner-only Salon preview and retention redemption remain covered by
the exact executable product contracts rather than a production account
mutation.

The three independent product reviews in `E-1028` returned GO with P0=0/P1=0.
Residual P2 limits remain documented there: client-asserted retention buckets
can exhaust only the bounded issuance quota, full contact scans need an index
at larger scale, the Kladovaya visual preview URL is not authenticated, and the
installer's post-check does not independently attest every SQLite schema
detail. None bypasses the first-order claim, increases the discount or exposes
student data.

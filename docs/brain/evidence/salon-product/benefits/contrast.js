async page=>{const out=[];const lum=c=>{const a=c.match(/[\d.]+/g).slice(0,3).map(Number).map(x=>{x/=255;return x<=.04045?x/12.92:((x+.055)/1.055)**2.4});return .2126*a[0]+.7152*a[1]+.0722*a[2]};for(const theme of ['light','dark']){await page.setViewportSize({width:1440,height:1000});await page.goto('http://127.0.0.1:8769/benefits.html');await page.evaluate(t=>document.documentElement.dataset.theme=t,theme);for(const selector of ['.bh-tabs a[aria-current]','.bh-verdict','.bh-plan.is-best .bh-plan-cta','.bh-plan-discount small']){const c=await page.locator(selector).first().evaluate(el=>{let bg=getComputedStyle(el).backgroundColor,p=el;while(bg==='rgba(0, 0, 0, 0)'&&p.parentElement){p=p.parentElement;bg=getComputedStyle(p).backgroundColor}return {fg:getComputedStyle(el).color,bg}});const l1=lum(c.fg),l2=lum(c.bg),ratio=(Math.max(l1,l2)+.05)/(Math.min(l1,l2)+.05);if(ratio<4.5)throw Error(selector+' '+ratio);out.push({theme,selector,...c,ratio})}}return out;}

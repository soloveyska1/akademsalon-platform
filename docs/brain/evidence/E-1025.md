# E-1025 — mobile configurator keyboard action shelf

- Date: 23 August 2026, Europe/Moscow.
- source_type: failing-first contract + Chromium/WebKit runtime geometry +
  real iOS Simulator software keyboard + production browser/hash/smoke and
  executed rollback/forward proof + full public regression.
- Canonical base: `f28b3ca79f17bd7bd29ea8eba481edb49c71c5e1`
  (`origin/main`, release164).
- Workstream: `WS-2f182483670947249a23af6655d28636`.
- Outcome: `OUT-008`; release gate: `G10`.
- Production status: published as `release165-5b1735c`; `current` and `dist`
  resolve to release165 and `previous` to `release164-1ea81c6` after an
  executed rollback/forward drill.

## Problem and bounded intervention

On the compact configurator, opening a mobile software keyboard hid the only
current-step action together with the full taskbar. A visitor could still type,
but had to close the keyboard and rediscover the primary before continuing.
This affected the exact high-intent practice-support route as well as the
generic configurator and could interrupt a qualified 14,000 RUB intent without
improving scope, trust or price understanding.

The change reuses the existing `#conceptTaskBar` primary as one compact shelf
above the software keyboard. It does not add a second CTA, route, submit path,
price, discount, timer or promise. Activation is page-local and requires all of
the following: mobile width, a focused input/textarea/select inside the current
wizard, software-keyboard occlusion above 140 px, a visible taskbar and no
consent bar.

The browser-specific fixed-position behaviour is measured before applying an
inset. If fixed content already follows the visual viewport, the inset remains
zero; otherwise it equals the obscured layout-viewport area. The active field
is scrolled only enough to stay above the validation reason and shelf. Closing
the keyboard restores the unmodified full taskbar.

The only related layout correction makes the mobile contact review and its
summary shrink within the viewport. It was exposed by the executable 40-character
boundary token; it does not change content, price or desktop geometry.

## Failing-first and deterministic proof

Before implementation, the focused contract failed 1/6 because the stylesheet
explicitly hid the taskbar during `keyboard-open`. The first real Chromium
runtime matrix failed at 320, 390 and 430 px because no taskbar primary was
visible or clickable.

After implementation:

- `node --test tests/compact-client-journey.test.js`: 6/6 passed;
- `node --test tests/*.test.js`: 592/592 passed;
- exact keyboard journey in Chromium at 320/390/430: 3/3 passed;
- exact keyboard journey in WebKit at 320/390/430: 3/3 passed;
- `node --check tests/mobile-light-smoke.js`: passed;
- `git diff --check`: passed;
- `./bin/brain validate`: `VALID records=88 links=164 manifests=39`.

Two independent final reviews of exact implementation
`8adcfb4543f7388f7b03403007771d63dae1e19f` reported GO with
P0=0/P1=0/P2=0. A commercial reviewer first requested an explicit transport
guard; the final test captures every POST during the journey and fails if the
list is non-empty. The product HTML/CSS did not change during that proof-only
strengthening.

The executable journey starts from the supplied-material practice-support
scope, 39 characters and a 14,000 RUB orientation. It proves:

- one visible primary; 39 characters keep the existing reason and disabled
  primary;
- the focused field is at least 16 px and stays above both reason and shelf;
- character 40 enables the same primary without losing focus;
- the button centre is hit-testable and one click advances to contact;
- contact keeps one disabled `Отправить заявку` primary, scope code
  `practice_draft_support`, unchecked authorship/consent and the 14,000 RUB
  orientation;
- a request listener captures every POST during the journey and requires the
  list to remain empty;
- restoring the native viewport removes keyboard state and restores the full
  taskbar;
- no request is submitted.

The broad legacy mobile smoke was also run across 42 page/engine/viewport
combinations. Its new keyboard cases passed 3/3 in Chromium. The aggregate
remained red (14/42) because of pre-existing unrelated waits/CORS and a 320 px
tariff overflow; those failures do not touch the changed configurator path and
are not reclassified as current proof. The isolated keyboard journeys above
are the release contract for this intervention.

## Real iOS and dark-theme observations

An iPhone 17 Pro simulator on iOS 26.4, 390 px class viewport, was exercised
with the actual Russian software keyboard. No synthetic submit was made.

- At 39/40, the textarea, validation reason and one disabled `К проверке`
  shelf were simultaneously visible.
- Typing one real key produced 40/40, removed the reason and enabled the same
  shelf while focus stayed in the textarea.
- Tapping that shelf closed the keyboard and opened the exact contact screen
  with disabled `Отправить заявку`.

Local screenshot SHA-256 values:

- 39/40: `c018a5baffa8bd8749274f21b9d69c01f14ea45e8ecdf0d72aab7a67587fc927`;
- 40/40: `546e0ede1eb97264dbb4f9381b8fe8d201486c9f1f50f668903d8a6a39c07f53`;
- contact: `876b45c80f72dff4e772be4b2dfab43b96f647ef594746ae85975d4c1d15c531`.

Exact final dark-theme Chromium geometry was rechecked after the final CSS
specificity guard:

- 360x800, visual bottom 464: shelf 395..464, field 230.89..328.89,
  reason 340.63..389, one primary, 16 px input, root overflow 0;
- 390x844, visual bottom 489: shelf 420..489, field 208.69..306.69,
  reason 365.63..414, one primary, 16 px input, root overflow 0.

At 1024x800 and 1440x900, `concept-keyboard-open` remained false and root
overflow was zero; the desktop taskbar stayed in its existing responsive mode.
Consent precedence was observed before accepting necessary-only cookies: the
consent bar stayed actionable and the keyboard shelf was not activated.

## Release165 production proof

Canonical source `5b1735c2c959ce63733b270259f74965d2fe46d3`
was built twice into byte-identical public payloads: 353 files / 25,048,287
bytes, digest
`51f755708d2537a2bf6904132604427c501378cdb5ebad06949fe128534d52e7`.
The immutable production tree `release165-5b1735c` contains 356 files /
25,058,458 bytes and has full-manifest digest
`7ea25e87ab5863333cebed04e6a90c9f5f574492084fd9bafafc3d01f7226e4c`.
Owner drift, directory-mode drift and file-mode drift are zero.

Exactly two public files differ from release164. Repository, immutable tree
and live HTTP agree on their SHA-256 values:

- `configurator.html`:
  `7f99eda2e4804ac6f33a86767bf7433e61b3ddaccafb4438db8b31fd50550cd4`;
- `assets/css/configurator-journey.css`:
  `d474c38e2198cee5be87c70bf76f82526092e23126eb05fc80874d77cbc43b1b`.

The three server-owned files retained their pre-release hashes:
`.indexnow-key` =
`f393d7a8fcf2a8a98418603755423a68b9b8f06536fbf18f167d98aafef3aaf8`,
`sw.js` =
`f500c8ff7786611a0b950c7d92d1315484cc30bfd023a1e5895bd8cbc8075648`,
and the growth notebook =
`0929cffc87336f376f586d651375e2f2863a89e66cdd8aec528e9d41c761b039`.
Backend, Nginx and Analytics v2 were not changed or restarted.

Production Chromium at 360x800 light and 390x844 dark repeated the exact
39/40 -> 40/40 journey. At 39 characters the reason and one disabled primary
were visible above the keyboard. Character 40 retained focus, hid the reason
and enabled the same 336x52 or 366x52 hit-testable primary. One click opened
contact with the exact `practice_draft_support` scope and 14,000–19,500 RUB
range; authorship and consent remained unchecked and submit remained disabled.
The network capture contained no POST. Root overflow was zero. A live iOS
Simulator route loaded the same production scope, while the actual software-
keyboard interaction remains bound to the pre-release iOS run above; exact
live HTTP hashes prove that production serves those reviewed HTML/CSS blobs.

After activation, both the operator-network and VPS GET/HEAD-only smoke passed
14/14. A real static rollback atomically restored `current` and `dist` to
`release164-1ea81c6` and `previous` to `release162-977c1f6`; both smoke
vantages again passed 14/14, live HTTP returned the old configurator/CSS hashes
`69ad276700d22a0a8ab91cf61ee13709a75f4e02499464e6b15f183e66ea7c0a`
and `78730db3fb482a1a13599d901d7ec7fe6786ff20c5fefb85f42578fbca10d703`,
and service/Nginx/SQLite stayed green. Forward restore returned `current` and
`dist` to release165 and `previous` to release164; both smoke vantages passed
14/14, the new hashes returned, and temporary pointer residue was zero.

The release backup is
`/root/site-backups/release165-keyboard-shelf-20260823T064936Z`.
No backend process was restarted and no production form was submitted.

## Privacy, commercial and inference boundary

No production contact, order, client file, message, payment, analytics row,
IP/UA, query/referrer or OAuth material was read or changed. No form was
submitted in local or simulated checks. The evidence proves reachability,
focus continuity, geometry, validation continuity and scope/price preservation;
it does not prove conversion uplift or revenue impact.

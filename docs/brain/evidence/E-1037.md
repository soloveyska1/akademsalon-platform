# E-1037 — REL-0173 retention-preview design publication proof

- Date: 25 August 2026, Europe/Moscow.
- source_type: exact candidate regression, two independent design/accessibility
  reviews, deterministic static manifest, immutable production readback,
  production Chromium inspection, two-vantage GET-only smoke and executed
  static rollback-forward drill.
- Canonical product source:
  `c245da0e00ce3a9dfaefbe985b9540672687e1d3`.
- Implementation result: `5ac8bc8f5af40b1d63fb4b9fd3e18413ffef5a27`.
- Product workstream: `WS-08d29437a63b4a6ebd06258dfbab076f`;
  publication workstream: `WS-3acdadf46aad4609bca4c5d7463302c0`.
- Outcome: `OUT-006`; gate: `G10`.
- Final static artifact: `release173-c245da0`; initial activation:
  `2026-08-25T16:35:03+03:00`; final forward activation:
  `2026-08-25T16:37:26+03:00`.
- Verdict: **GO** for the bounded retention-preview design correction,
  P0=0/P1=0. This does not close unrelated SEO, legal, pricing or historical
  test-harness debt.

## Released outcome and invariant boundary

The long `Вернёмся к предварительному итогу` outcome that clipped outside the
retention sheet is now the concise `Итог уже готов`. Retention-only typography
is capped at 46 px with balanced wrapping. Outcome actions use a 360 px
editorial column, 340 px on phones, rather than a full-width black slab. The
primary uses the Salon wax color; the secondary and factual terms remain
visually subordinate. Dark hover and Windows forced-colors keep exact readable
text/background/border roles.

The sheet resets only its own inner scroll when moving between the reason list
and an outcome. This prevents a previously scrolled price branch from hiding
the owner label and close control. Focus, Browser Back, page history and the
underlying application state remain unchanged.

The owner-only route remains explicitly labelled and presentation-only. It
cannot issue or claim the 10% offer, navigate through its client-looking action,
write promo state or alter statistics. Eligibility, the four reason decisions,
server recheck, best-of rules and all REL-0169 economics remain exact. Only an
explicit price objection can request the existing 10% offer from 5,000 RUB,
capped at 2,500 RUB for 72 hours.

No backend, bot, database, API contract, tariff, price, deposit, Salon+,
referral or Kladovaya artifact changed. `deposit.html` remained byte-identical
at `161876c7ab1ac5a19cd9a3f3f1b88db9129c3909184418295d533c87631a83b8`;
deposit issuance remained open throughout REL-0173.

## Independent review and pre-publication proof

- Final visual review: GO, P0=0/P1=0/P2=0. Fresh 568 and 1440 light captures
  and dark captures show no clipping, slab CTA, owner-label collision or lost
  action on short height.
- Final accessibility/behavior review: GO, P0=0/P1=0. Chromium forced-colors
  light/dark normal/hover resolves primary text/background/border to
  CanvasText/Canvas/CanvasText and secondary to CanvasText/Canvas. Dark hover
  contrast is about 7.29:1.
- Focused promo/readiness contracts passed 14/14. Full public Node passed
  623/623. Brain passed 39/39 and strict validation reported
  `VALID records=107 links=209 manifests=54` before the release records.
- WebKit and Chromium light/dark/reduced-motion promo journeys passed 24/24 at
  320/390/430/568x514/760x650/1440. All four outcomes remain inside the sheet,
  title is <=46 px without overflow, primary is >=49 px and secondary targets
  are >=44 px. Owner actions make no POST, storage, navigation or claim.
- Two clean builds were byte-identical: 358 public files, 26,237,792 bytes,
  relative path-and-content manifest
  `94fe23488b99eb1b30d896a1346f80def24897b782f8f566e1551b18448c736f`.
- Accepted P2 is pre-existing test debt: the default mobile smoke index
  inspector still waits for removed legacy `#toc`/`Salon.toc`. The dedicated
  promo-only matrix is the exact green release gate for this surface.

Exact changed public hashes are:

- `assets/css/promo-campaign.css`
  `420a0fb4915dd5bc098631be1c9a6b477649fcc380ae9d0ca01577869465e765`;
- `assets/js/promo-campaign.js`
  `9e9d283494bcc3ba3a417c5d7bbcbbbbbd353e55015b68aae38df494bda8a1f7`;
- `index.html`
  `6e4008b6c00d5e4a4db4cf50dfc0a46565d58ea0101ae6ae8cd138cd45d17bcf`;
- `configurator.html`
  `4b382f215f78b3bc03b6ed998eb6aa0db16e740293bf3997dfef1e791548c60b`.

## Static publication and exact remote state

The inactive tree at
`/var/www/academic_saloon/releases/release173-c245da0` was assembled before any
pointer change. It contains 361 files / 26,247,963 bytes: the exact 358-file
public build plus unchanged `.indexnow-key`, `sw.js` and
`analysis/growth-baseline-2026-07-24.ipynb`. The full release manifest is
`40ea966bd2dd6c10f9cf9990e299da490856648c74c90e921fad53ee2bdd4dda`.

All 361 files are `www-data:www-data` mode 0644, all 18 directories are mode
0755, and owner, mode, path and symlink drift are zero. Retained hashes remain
`f3930289...8af8`, `f500c4f3...5648` and `0929da3e...b039`. The exact pointer
backup is
`/root/site-backups/release173-retention-preview-20260825T133327Z`.

Initial activation set `current=dist=release173-c245da0` and
`previous=release172-df007e4`. The first origin observation used `curl | grep
-q`; early consumer exit made curl return code 23 after the pointers had
already switched. No release file or pointer was wrong. A bounded temporary
download then read all five exact expected HTTP hashes and closed this observer
error before external testing.

## Production browser, rollback and final health

Isolated production Chromium inspected owner preview at 390x844 and 568x514.
For both `unclear` and `price`, title overflow, sheet overflow and root overflow
were zero; sheet scroll was zero; title focus was correct; actions were 340 px;
primary was 50 px; secondary was 44 px; owner label and close were visible.
The CTA was exact wax `#A63F29`, not the old black slab. URL and browser storage
were unchanged, console errors were zero and all 32 observed requests per
viewport were GET. Only safe GET auth/eligibility responses were controlled to
reach the otherwise owner-only presentation; no value-creating response was
mocked.

External and VPS `production-smoke.js` each passed 14/14 after initial
activation. At `16:37:09+03:00`, the real rollback restored
`current=dist=release172-df007e4` and `previous=release171-ffa2421`; exact old
index/configurator/promo hashes returned and both vantage points passed 14/14.
At `16:37:26+03:00`, forward restore returned
`current=dist=release173-c245da0` and `previous=release172-df007e4`; exact new
hashes, both 14/14 smoke runs and the production browser matrix passed again.

`salon-bot-v2.service` remained active as PID `557663`, `NRestarts=0`, with its
unchanged `13:06:18` start time. Nginx is valid. SQLite remains WAL with
`quick_check=ok`; final health is `ok` with 35 active orders. Since the
pre-release snapshot, actual `database is locked`, traceback, ERROR or CRITICAL
journal matches are zero.

REL-0173 proves the exact corrected presentation, non-mutation boundary and
rollback readiness. It does not prove conversion, revenue, contribution margin
or profit uplift; those require an authoritative consented sample and
fulfilment-cost data.

# E-1021 — Release160: цена практики понятна до разговора

- Date: 23 August 2026, Europe/Moscow.
- source_type: exact canonical build + immutable static release + two-vantage
  GET/HEAD-only smoke + real Chromium + executed static rollback/forward.
- Canonical production source: `135cb4559ee6c81b0e42633d2ebb2ad48abbb1a1`.
- Verified product result: `89a2887eff51561e519c1357e5da88046b9c9d9c`;
  implementation fix: `8a1d70e40f307579e9841bf16fb3aacb1465001a`.
- Workstreams: `WS-d77d10e6e07a4642b41873171f873b19` and
  `WS-5948fc36e7634bc2936a1d53398d4216`.
- Outcome: `OUT-006`; release gate: `G10`.
- Production release: `release160-135cb45`.

## Пользовательский результат

Публичная страница отчёта по практике больше не прячет разницу между ценой
редактуры и сопровождения до личного разговора. Клиент выбирает один из трёх
объективных объёмов:

- письменный разбор — от 2 500 RUB, без правок в документе;
- редактура готового фактического комплекта — от 8 000 RUB;
- сопровождение черновика и связанных материалов по этапам — от 14 000 RUB.

Выбор меняет подпись, состояние и оба дальнейших CTA. В боевом браузере
`draft+support` открыл «Сопровождение исследования по этапам», показал первый
результат и диапазон от 14 000 до 19 500 RUB. Цена 14 000 RUB объясняется как
другой состав, а не как внезапное удорожание той же редактуры. Направление,
требования и срочность названы факторами повышения; фиксированная цена этапа
появляется в спецификации до оплаты.

Страница сохраняет право остановиться между этапами, ограниченный совместимым
продолжением 14-дневный зачёт письменного разбора и авторский контур студента.
Скидка, искусственный дефицит, гарантия оценки, выдуманный кейс или обещание
uplift не добавлялись.

## Source и deterministic proof

На exact canonical source зелёные:

- public product 584/584;
- backend 31/31;
- Brain 39/39;
- strict Brain: 78 records / 143 links / 31 manifests;
- focused practice/SEO/configurator/context contracts 32/32;
- `git diff --check`, full build and two independent final reviews без P0/P1/P2.

Две public-сборки byte-identical: 353 файла / 25 006 595 bytes,
deterministic digest
`4bba587fbbf9c5c2b3213d54392957f17bb703bbd3851d2c74b4faa7136efcdf`.
Existing owner-only Sites preview version 36 was saved from and deployed for the
same canonical commit before public activation.

## Production preflight и immutable payload

До mutation `current=dist=release159-57703fa`,
`previous=release158-fa2b317`; service active, Nginx valid, SQLite `ok`,
external and VPS smoke 14/14 each. Webapp, DB seam, Analytics runtime/contract,
incident-hardened Nginx and security-header hashes were bound before switch.

Checksum dry-run against release159 contained exactly two public changes:

- `otchet-po-praktike.html` →
  `518df2272137840182075cf6a933bc0048e9b72b113efb3ea7138a7c7bec6a05`;
- `assets/css/polish15-catalog.css` →
  `a1109bda0566f3d3a484f18ad3a136e187c3912f657f8c6924c632c76fb268e4`.

The established server-owned `.indexnow-key`, SEO notebook and `sw.js` were
copied with their exact release159 hashes. Inactive release160 then had 356
files / 25 016 766 bytes, full streamed manifest digest
`4b6c07fbc8a86cd64dede460680a76f1bb687a34ab9a85ac0f42787bf00c2e9b`,
353/353 source hash parity and zero owner/mode drift before activation.

## Live browser, HTTP and rollback

Repository, immutable release and external/VPS HTTP SHA-256 agree for both
changed files. Real production Chromium at 390 and 1440 CSS px in light and
dark themes showed all three prices, three native radios, `draft+support`, zero
root overflow and zero console errors/warnings. Structured data exposes three
minimum-price Offers `[2500, 8000, 14000]` and no false `highPrice`.

After activation external and VPS smoke passed 14/14. A real rollback moved
both static pointers to release159 and `previous` to release158; old page/CSS
hashes returned and both smoke vantages again passed 14/14. Forward restore
returned both pointers to release160 and `previous` to release159; final
external and VPS smoke passed 14/14 again. Backend was never restarted or
modified.

Final state at `2026-08-22T23:35:13Z`:

- `current=dist=release160-135cb45`;
- `previous=release159-57703fa`;
- service active, Nginx valid, SQLite `ok`;
- immutable file/hash/owner/mode checks green;
- backend/Nginx hashes byte-identical to preflight;
- remote smoke temp residue: 0.

Ignored browser evidence is under
`output/playwright/release160-practice/`.

## Privacy и граница вывода

Ни один production contact, order, file, message, raw analytics row, IP/UA,
query/referrer или OAuth material не читался и не менялся. Проверки выполняли
только GET/HEAD и системные health/hash/read-only SQLite операции. Админка,
аналитика, calculator values, form submission и backend не изменялись.

Источник отказавшегося лида остаётся неизвестным: этот релиз исправляет
ценностную упаковку, но не приписывает лида каналу и не доказывает рост
конверсии. Такой вывод допустим только после достаточного consented sample по
заранее выбранному окну.

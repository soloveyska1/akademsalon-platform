# E-1001 — Brain 1.5 deterministic contract proof

- Date: 2026-08-02, Europe/Moscow
- source_type: deterministic-local
- Base: `da0a05e83b0cf98d931820aa1468160e2ac66df0`
- Branch: `codex/brain-v2-out-001`
- Scope: project-memory tooling and tracked contracts only; no product runtime,
  production data, OAuth or deployment. Explicit Git-origin integration was later
  authorized; force-push remains forbidden.
- Product anchor: `OUT-001`; architecture decision: `DEC-0007`.

## Reproducible observations

1. Failing-first baseline: the first Brain test discovery stopped with four import
   errors because `BrainFailure` and `BrainProject` did not yet exist.
2. Final isolated suite:
   `python3 -m unittest discover -s tools/brain/tests -p 'test_*.py' -v` →
   39 tests, 39 pass, 0 fail.
3. Product regression: `node --test tests/*.test.js` → 430 tests, 430 pass,
   0 fail, 0 skipped.
4. `./bin/brain validate --strict` passed on the tracked corpus and one manifest.
5. Two consecutive `context --id OUT-001` runs on the same tree produced
   byte-identical SHA-256 output. Mandatory overflow is separately covered by a
   fail-closed unit test; no record is byte-sliced.
6. `./bin/brain refresh` built SQLite v2 by temporary integrity/size validation
   and atomic replace. Observed modes: `.brain` 0700, `index.sqlite` 0600.
7. Focused failing repros proved that a forged terminal status, post-submit drift
   and an explicit foreign current manifest could falsely clear. Final regression
   tests require canonical/result ancestry, exact manifest-only closure drift and
   current branch ownership; legacy v1 never releases scope without migration.
8. `./bin/brain doctor` reported canonical validation OK, current index and no
   Brain background service. `./bin/brain processes` also exposed pre-existing
   Playwright processes; they were not started, modified or stopped by this work.
9. Workstream bootstrap creates schema v2 manifest plus branch-local handoff;
   context includes the complete active handoff. Atomic replace failure injection
   preserves the previous manifest bytes.
10. Conflict reports discover declarations from local branch refs, compare them
    with actual diffs, inspect active/detached worktrees, aggregate dormant refs
    and expose matching text/JSON decision, blocking, hard/warning/info counts and
    exit code. Disjoint historical/environment observations are INFO; actual
    dormant overlap remains warning, while active/dirty/ambiguous state is hard.
11. Regression coverage advances the canonical ref after manifest creation: the
    exact snapshot digest changes, declared write overlap becomes
    `BASE_SCOPE_DRIFT`, and a disjoint stale base remains a visible warning.
12. Legacy migration is explicit and SHA-free: `workstream migrate` reopens an
    unfrozen terminal v1 record as active, creates its branch-local handoff and
    adds only that exact file to declared write scope.

## Council connectivity and observations

- Doctor: Kimi, Sonnet, GLM, Opus and Fable `READY`; `local_llm: DISABLED`;
  `background_services: NONE`.
- Probe from this worktree: all five providers `READY`.
- Daily independent review used Kimi+Sonnet+GLM. One Opus review was used for the
  tracked-Markdown versus second tracked graph fork. Fable was not used for review.
- Daily feedback caused mandatory fresh fetch guidance, strict-by-default
  conflicts/bootstrap, unconditional failure for ambiguous foreign manifests and
  explicit record schema changes. Opus confirmed that tracked Markdown plus
  local ref manifests and a sequential integration owner are sufficient at the
  current scale; a remote lease/merge queue needs evidence of repeated canonical
  serialization failure. The final suite also proves custom `prefix` namespaces
  are taken from policy rather than a hardcoded list.
- Council files remain under ignored `.brain/council`; these observations do not
  close a hard gate and contain no tracked credentials.

## Result and limits

The Brain 1.5 integration candidate is reproducible and fail-closed for its declared
scope. It does not know unfetched, unpushed or undeclared work; typed semantic
relations, remote leases and merge queues remain explicitly unimplemented.
Production `OUT-001` submit/delivery/cabinet proof is still unexecuted and needs a
separate safe marker/cleanup plan.

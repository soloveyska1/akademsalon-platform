# E-1022 — Release161: цена, заявка и спецификация говорят об одном объёме

- Date: 23 August 2026, Europe/Moscow.
- source_type: exact canonical build + deterministic route/request/specification
  contracts + immutable static release + two-vantage GET/HEAD-only smoke + real
  Chromium + executed static rollback/forward.
- Canonical production source: `1c5f0eed53da5ab894c06545ae80806790fb1d8c`.
- Verified product result: `34e10367eac4f7451b56bbe01753ff00e9dffce2`.
- Workstreams: `WS-f66ad05f7d864b93a4f3d60646050847` and
  `WS-f9ec27f9f3b24bfe81bd24b1d77f0b58`.
- Outcome: `OUT-006`; release gate: `G10`.
- Production release: `release161-1c5f0ee`.

## Пользовательский результат

Release160 made the 2,500 / 8,000 / 14,000 RUB entry scopes visible before a
conversation. Release161 closes the downstream continuity gap: the scope a
visitor chose is now the scope shown on the next screen and carried into the
request and prepayment specification.

The exact live contracts are:

- `practice_draft_diagnostic` — A1 consultation, 2,500–3,500 RUB, a map of
  mismatches, mandatory corrections and priorities; the exclusions explicitly
  forbid document edits, invented facts, grade guarantees and submission for
  the client;
- `practice_draft_editing` — A1 supplied-material editing, 8,000–11,000 RUB,
  Word with visible changes, program/methodical check and a checklist of
  signatures and applications;
- `practice_draft_support` — A1 supplied-material staged editing,
  14,000–19,500 RUB, requirements map and stage plan, agreed document versions
  and final completeness checklist.

Each exact scope carries `result_code`, `scope_code`, canonical deliverable,
inclusions, exclusions and required inputs. Direct submission materializes one
exact cart row even if the drawer was never opened. The admin specification
resolver accepts both direct `items` and raw `cart.items`, honors explicit A1
before the legacy VIP/A2 heuristic and retains `required_inputs` as a visible
customer-input dependency. A raw generic `topic+support` request without an
exact practice scope still remains A2 and keeps the lawful from-zero contract.

No real request, contact, file, payment or production admin row was created to
prove this release. The request/specification seam is covered by executable
contracts and intercepted local POST bodies.

## Source and deterministic proof

On exact canonical product source:

- public product 589/589;
- backend 31/31;
- Brain 39/39;
- focused practice/cart/specification 25/25;
- strict Brain validation, JavaScript syntax and `git diff --check` green;
- three independent final reviews: P0=0, P1=0, P2=0.

Two `dist/client` builds are byte-identical: 353 files / 25,030,042 bytes,
deterministic digest
`00cc9703122189d93aac2c08adcb3a41c0069e10ba7c85cebfb50493c2a36952`.
The full Sites artifact is 354 files / 25,030,981 bytes, digest
`946eedded541877a10cf4ba41d7f9d2b54618786666d2c91faa23594c65c15e3`.
Owner-only protected Sites preview version 37 was saved and deployed from the
same canonical source before public activation.

## Production preflight and immutable payload

At `2026-08-23T01:30:13Z`, before static mutation:

- `current=dist=release160-135cb45`;
- `previous=release159-57703fa`;
- service active, Nginx valid, SQLite `ok`;
- external and VPS read-only smoke 14/14 each;
- target and staging release161 paths absent.

The inactive release was assembled from the immutable release160 baseline and
the exact public-client build without deleting server-owned assets. Checksum
comparison against release160 reported exactly five content changes:

| Public file | release160 SHA-256 | release161 SHA-256 |
| --- | --- | --- |
| `admin.html` | `b3d86dd1008ed30832cf74231a306dac33410002560aad9c1357bff91e92101f` | `242763c2a82c45a72f7fc00acaf38d41bf742ff53e39eafe4179e13fc2ef0a9d` |
| `configurator.html` | `acf6527b8867faab835d34ee6a4754c6a2c6fdcbfc922c7720ae128f1f884341` | `69ad276700d22a0a8ab91cf61ee13709a75f4e02499464e6b15f183e66ea7c0a` |
| `assets/css/configurator-journey.css` | `97bd43a747eee983f4b281d51bd14f668db1bd4737d71b34f879051e310ffde6` | `78730db3fb482a1a13599d901d7ec7fe6786ff20c5fefb85f42578fbca10d703` |
| `assets/js/admin.js` | `875176c98c09e2d25e54c8cc9cd7bd04f0d73f1b41368b9ce5c3ca85ad88078d` | `938153eeb0cb2a8dbdff4b1e676186b6869377a64129e1510b2030de8240ee79` |
| `assets/js/cart.js` | `b46c83a7744440f4061e5ee6b5743dc2feec8312d9ea1c7ddd30fc298d7b6c22` | `4a9f49fc28ec71fc035228aad04dec06b7845a9299ca8120d0255db9a1077900` |

Repository, `dist/client`, inactive release and later live HTTP hashes agree
for every release161 value. The five changed files have distinct inodes from
release160; release160 content hashes remained unchanged.

The server-owned files were preserved exactly:

- `.indexnow-key` —
  `f3930289da0464745613a2dd2d345d24cd78c60e665f775c2c07320d4bad8af8`;
- `analysis/growth-baseline-2026-07-24.ipynb` —
  `0929da3eb83ab650bd742639a61beaf2828ed81420d95bc7806c0d1ff8deb039`;
- `sw.js` —
  `f500c4f386717e39ff85dbf40ec7dac1a855b1044321081e05e67f2787275648`.

Inactive and final release161 have 356 files / 25,040,213 bytes, full streamed
manifest digest
`a455a53b8780c4395f71d9f4021e6d00d4584951fee600f696f8cfd6035023f4`,
353/353 source parity, owner/mode drift 0 and no unexpected public addition or
deletion.

## Live browser and route proof

Release161 first activated at `2026-08-23T01:36:08Z`. Exact HTTP SHA-256 for all
five files matched the repository and immutable tree.

Real production Chromium proved:

- public price selector at 390 and 1440: three native choices, visible
  `от 2 500 ₽`, `от 8 000 ₽`, `от 14 000 ₽`, one primary continuation and root
  overflow 0;
- diagnostic at 390: exact title, mismatch map, `без правок в документе`,
  diagnostic upload request and summary before fields;
- editing at 390: exact title, Word/program/checklist output, ready-package
  upload request, exact ready-report/diary hint and summary before fields;
- support at 390 and 1440: exact title, requirements-map/stage-plan output,
  14,000–19,500 RUB range and package-material upload; neither `Можно прийти
  без текста` nor `План исследования и задачи на первый этап` appears;
- desktop keeps the established form-left / dossier-right composition;
  390/1440 light/dark have root overflow 0 and console errors/warnings 0.

The secondary `Изменить объём` link resolves to
`otchet-po-praktike.html#service-price`; it does not reset the visitor into the
generic configurator. Ignored screenshots and snapshots are under
`output/playwright/release161-practice/`.

## Executed rollback and final state

After activation, external and VPS smoke passed 14/14. At
`2026-08-23T01:42:51Z`, both static pointers were moved to release160 and
`previous` to release159. The five old release160 HTTP hashes returned, while
external and VPS smoke again passed 14/14.

At `2026-08-23T01:43:55Z`, forward restore moved `current` and `dist` to
release161 and `previous` to release160. Final external and VPS smoke passed
14/14. A post-restore browser reload at 390 and 1440 retained the exact support
scope, dark theme, zero overflow, desktop columns and zero console errors.

Final production truth:

- `current=dist=release161-1c5f0ee`;
- `previous=release160-135cb45`;
- service active, Nginx valid, SQLite `ok`;
- immutable files 356 / bytes 25,040,213 / manifest `a455a53b…23f4`;
- repository and live HTTP hashes agree for all five public changes;
- temporary pointer residue 0.

Backend and infrastructure hashes remained byte-identical throughout:

- `webapp.py` —
  `6c30cf2f02fcda1247131cc87078bb643307b5746880825bf7b6bec4fce55c05`;
- `db.py` —
  `7611e08b69bbe283b5614fbaaf5bd3f379b95cefb57dcc033b23446ca051dad9`;
- `analytics_v2.py` —
  `69a9d642263a88570abadc7f56c06138834c573d6f0049b1d7d02b8bc28991ea`;
- `analytics_contract_v2.json` —
  `e05bfd4b5b7588f35815ef32de06760e3ec2400b08e6790a9d6c268058021f99`;
- active Nginx site —
  `e66545c320c7ac2c2f84f76a3d4b9d87c216179fdd67388e573ea3e1d489003d`;
- security headers —
  `ebe20503129e3b856f9e3d645ee206877dcc067d21fe7f6b2597c4b2b8e6d77d`.

The backend service was not restarted or modified.

## Privacy and inference boundary

No production contact, order, client file, message, payment, raw analytics row,
IP/UA, query/referrer or OAuth material was read or changed. Public checks used
GET/HEAD only; system checks used hashes, service state, Nginx syntax and
read-only SQLite integrity.

The source of the rejected lead remains unknown. This release protects scope
and price continuity but does not prove conversion uplift. A later result
passport may reuse only the explicitly fictional demo specification and must
not invent a practice-specific review, acceptance outcome or fixed package.

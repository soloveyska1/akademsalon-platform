# E-1038 — REL-0174 OUT-001 production journey and cleanup proof

- Date: 26 August 2026, Europe/Moscow.
- source_type: exact canonical source and asset hashes, failing-first regression,
  stopped-service install, bounded digest-only HTTPS probe, aggregate SQLite
  readback, two-vantage GET-only smoke and executed rollback-forward.
- Canonical repository source:
  `d6f1a1b4783d5600abcfe5ecce835c56e5c3bac4`.
- Authoritative implementation result:
  `7cf25718e9c8ae39c1e7105b62d3206e4e8196b7`; generated-link hotfix result:
  `292784c11aeadfd860e660d9c2f4147e19d74069`.
- Implementation workstreams: `WS-4d34f0a468874c12bdf39703834c194f`,
  `WS-112f390ab1204285994b4c90e84ed464`,
  `WS-67ebb92f28c4457d9fc1a59ae4470d5c` and
  `WS-2fbadbef8bd1445f9e57de6f44d06249`; final publication workstream:
  `WS-dda370b659e74042941fd82fd6de984d`.
- Outcome: `OUT-001`; gate: `G10`.
- Final forward activation: `2026-08-26T05:12:08+03:00`.
- Verdict: **GO**. Three independent final reviews report
  P0=0/P1=0/P2=0 for this frozen production evidence.

## Boundary and exact release set

REL-0174 adds a default-off synthetic plane to the authoritative backend. A
request is synthetic only when the exact non-client fixture carries a fresh
canonical-origin HMAC from a root-owned short-lived capability. It has no
contact, user, customer text or file and rejects promo, gift, referral, bonus,
deposit, payment and real delivery state before database work. Ordinary
requests never read the capability.

The final exact set is:

- installer:
  `031bedbea5a8a51e4a1409da473afafff0369e1d2305e2afc2d1726da2b0d186`;
- `app/db.py`: `b9ac6409c834f6858855d13aba862e3c0dc063c064837e4f5c5d5b75ea4efd6f`;
- `app/webapp.py`:
  `cb5b2624f9ed9769e9464f2a198ad997127641fba6fa19d30cec9fbc3943a200`;
- `app/out001_synthetic.py`:
  `cba09cf5db96d632d3f07ff45713b9518841b4a6654217bec3ef9f9fa87844a5`;
- `out001_probe.py`:
  `5dd4cb602f92f4a36511376336b12076a9e0482ec7387ff99c3e30f69ebb452d`;
- `migrations/0010_out001_synthetic.sql`:
  `e6500fa4dd68a2a99f357f0a5c8b118d05e12858980354a82452fe0d80cbf8d8`.

Runtime and migration are root-owned `0640`; the probe is root-owned `0700`.
The order-schema digest is
`c7b91d09c4a0f4f1ff737d1c650bca508cdb70ba2977bd9ede9775829ca941bb`;
the required surface schema is exact and migration `0010_out001_synthetic` is
recorded once.

No public/static artifact changed. Production `current` and compatibility
`dist` remain `release173-c245da0`; `previous` remains
`release172-df007e4`. Prices, tariffs, promo presentation, deposit UI, public
design and Kladovaya were not changed.

## Security stop and generated-link hotfix

The first bounded production journey completed at `04:18–04:23+03:00` with
runtime `7add4843…28cf`, one exact cleaned tombstone and an unchanged economic
guard. Its candidate GO was then withdrawn when independent security review
showed that `_order_links()` used `PRAGMA table_info` and could miss a future
generated or hidden `order_id` column. A live read-only comparison immediately
found `table_info=30`, `table_xinfo=30`, no hidden order links and no unknown
links, so that already-completed cleanup had no residue; emergency production
rollback was not required.

A dedicated generated `order_id` fixture failed before the hotfix with
`CleanupBlocked not raised`. Runtime result `292784c1…4069` switches only the
global link inventory to `PRAGMA table_xinfo`; the final regression requires
exact `synthetic_schema_drift` and proves order=1, foreign row=1 and
tombstone=0. Independent VIRTUAL (`hidden=2`) and STORED (`hidden=3`) checks,
including insertion between dry-run and apply, also blocked before deletion.

The installer runtime pin advanced atomically. Exact pinned-source fixture
`/tmp/out001-source.twXZrq` used source hashes `51702018…b2a1` and
`6f36199c…123c`. Results after the final test refinement:

- focused backend 30/30;
- full backend 129 discovered, 128 pass and one exact environment skip because
  `aiosqlite` exists only in the production venv;
- public 623/623; Brain 39/39;
- strict validation `VALID records=109 links=216 manifests=59` before the final
  evidence records were added.

Final security/trust, economics/eligibility and product/ops reviews all returned
GO with P0=0/P1=0/P2=0 after their documentary P2 requests were closed. The
exact failure reason is asserted, the release boundary is explicit, tombstones
are reconciled per journey and in aggregate, and the two-installer transition
below was rehearsed and recorded.

After the evidence freeze, the exact pinned-source fixture repeated focused
30/30 and full backend 129 with the one production-venv `aiosqlite` skip;
public repeated 623/623, Brain repeated 39/39, `git diff --check` was clean and
strict validation returned `VALID records=111 links=223 manifests=60`.

## Exact transition rehearsal

Direct new-installer apply from the previously installed runtime is forbidden
and reproduced as fail-closed:
`OUT-001 asset target already exists with incomplete install`. New-installer
rollback also cannot consume an old asset manifest. The accepted local
rehearsal used:

1. old installer `296c21c3…7883` and old runtime `7add4843…28cf`;
2. old rollback to exact source preimages and all three targets absent;
3. new installer `031bedbe…d186` apply over the retained single migration row;
4. immediate `changed=false`, new rollback, new forward and another
   `changed=false`;
5. final exact source and asset maps shown above.

Production therefore used only the same stopped-service chain with old backup
`/root/salon_bot/backups/out001-synthetic-20260826T012302978117Z`. That backup
was read before mutation: root-owned `0700`, exact preimages and exact old asset
manifest.

## Final production transition

Read-only preflight at `2026-08-26T05:06:19+03:00` observed PID `755036`,
`NRestarts=0`, health `ok`, 35 active ordinary orders, exact old post-image
hashes, capability absent, active synthetic/outbox/receipt zero and one cleaned
tombstone. SQLite was WAL with `quick_check=ok`, zero FK violations, exact
schema, `table_info=table_xinfo=known=30`, no hidden links, deposit issuance
`earned-v2:open` and economic guard
`49cf27f701efe73c10627ccd3dce0c7af0d4690f6f2db67e5549a7f2595172a3`.
External and VPS smoke each passed 14/14.

At `05:09:16+03:00`, `salon-bot-v2` was stopped and verified inactive. Exact
old rollback restored source preimages `51702018…b2a1` and `6f36199c…123c` and
removed all three old assets. New installer check returned `changed=true` with
the migration already applied. New apply created backup
`/root/salon_bot/backups/out001-synthetic-20260826T020917333126Z`; the immediate
repeat returned `changed=false`. The service became healthy at
`05:09:17+03:00` as PID `768252`, `NRestarts=0`; external and VPS smoke each
passed 14/14.

## Repeated bounded journey and cleanup

The exact root-only probe printed one sub-4 KiB digest-only JSON document and no
raw ID, contact, cookie, capability or claim state. Its final result was:

- `state=passed`, `proof_ready=true`, `outbox_state=done`, blockers empty;
- counts: order 1, marker 1, created event 1, outbox 1, receipt 1, guest
  session 1, cabinet membership 1, claim exchange 1, item 0, file 0;
- duplicate submit resolved to the same private order;
- runtime and migration hashes matched the exact release set;
- run hash
  `443e0f88487dd6399026cebe5668387e66d407b612d19197594c9a3bc7b97a28`;
- order-reference hash
  `f74c8354598d240761d76548df2cfd4a035bae6f1505bc1b834e088d3d5b9102`;
- contract hash
  `a9c8a8b0b282dfc874183641f02322395fcbd3baa5b85a2547cb259dcae43141`;
- cleanup dry-run digest
  `34f7cef0cc4659bc10940f2a3cf0f39b35feb2ebb7443222e0f04284003dfc5b`.

Cleanup reported `active_zero=true`, `economic_guard_unchanged=true`, one exact
tombstone and a second no-op. Aggregate readback found zero synthetic orders,
zero `out001_synthetic` outbox rows, zero isolated receipts, capability absent
and the exact new tombstone `[surface_mask=255, contract_version=1,
result=cleaned]`. Total tombstones are two, one per executed bounded journey.
Delivery ended in the DB-local isolated receipt path; no external channel was
addressed.

## Executed new rollback and forward

At `05:11:06+03:00`, stopped-service rollback through the new backup restored
both exact source preimages and removed all three assets. The additive migration
and two opaque tombstones intentionally remained; active synthetic/outbox/
receipt counts stayed zero, `quick_check=ok` and FK violations zero. Service PID
`768747` became healthy with `NRestarts=0`; external and VPS smoke each passed
14/14.

At `05:11:58+03:00`, forward apply recreated the exact post-image and final
rollback copy
`/root/salon_bot/backups/out001-synthetic-20260826T021208015769Z`. Immediate
idempotence returned `changed=false`. Final activation was
`05:12:08+03:00` as PID `768912`, `NRestarts=0`; both smoke vantage points again
passed 14/14. Final installer check returned `changed=false` and
`migration_applied=true`.

## Stabilized state and journal boundary

At `05:13:51+03:00`, health was `ok` with uptime 100 seconds and 35 active
ordinary orders. PID `768912` owned listener `127.0.0.1:8090`; Nginx was valid.
SQLite remained WAL, `quick_check=ok`, FK violations zero, order schema exact,
`table_xinfo` links exact 30/30, hidden links none, active synthetic/outbox/
receipt zero, capability absent and tombstones two. The economic guard remained
byte-identical and deposit issuance stayed open. Static pointers were read back
as release173/release173/release172.

The hotfix window contained zero `database is locked`, traceback or CRITICAL
messages. From `05:13:50` through `05:14:00`, aiogram logged six upstream
Telegram `Bad Gateway` polling errors while HTTP health, SQLite and PID remained
stable; this path was not addressed by the isolated probe. Polling recovered
without a service restart, with zero further Telegram/error/lock/traceback/
critical matches after `05:14:05` through `05:19:13+03:00`. At that readback
health was still `ok`, uptime 422 seconds, PID `768912`, `NRestarts=0`.

A final read-only freeze at `05:30:25+03:00` observed the same PID with uptime
1095 seconds and `NRestarts=0`, 35 active ordinary orders, exact release hashes,
zero active synthetic/outbox/receipt rows, capability absent, two tombstones,
30/30 exact links with none hidden, unchanged economic guard, deposits open,
WAL, `quick_check=ok`, FK violations zero and unchanged static pointers. Since
`05:14:05`, exact `Bad Gateway`, `database is locked`, traceback, uppercase
ERROR and CRITICAL matches were zero. A separate reviewer observed one later
INFO-level operational line containing the lowercase literal `error`; it was
not a Telegram/DB exception, service failure or restart.

Temporary staging was the single exact root-owned path
`/root/out001-release174-final.XNHTAs`; after the stabilized clean-window
readback it was validated as non-symlink `0:0/0700` and removed by that exact
path. All OUT-001 source backups are retained. No SQLite snapshot was restored
or deleted.

## Result and limits

This proves the bounded technical production journey, server-confirmed
idempotency, isolated delivery, cabinet visibility, generated-link-safe cleanup
and release rollback mechanics. It does not prove organic-client conversion,
external Telegram fulfilment, operator response quality, revenue, margin or
profit uplift. Future probes remain explicit root-only operations, not demo
fixtures or background traffic.

# E-1020 — Release159: цена следует выбору без визуальной регрессии

- Date: 23 August 2026, Europe/Moscow.
- source_type: exact production preflight + immutable release + two-vantage
  read-only smoke + real Chromium + bounded synthetic ingest/revoke + executed
  full rollback/forward restore.
- Canonical source: `57703fa7d97fb8ad9b8685dda0a6e684801e939d`.
- Quote-scope implementation: `5ef9b235d970969b5479d1708a1fdb7e9b3c0db6`.
- Workstream: `WS-b4ec6d890eab488784d19eecf7cdca2e`.
- Outcome: `OUT-006`; release gate: `G10`.
- Production release: `release159-57703fa`.

## Пользовательский результат

На живом конфигураторе строка цены теперь прямо соответствует выбранному
объёму:

- `Только первый этап` сохраняет известный ориентир первого этапа;
- `До следующего рубежа` показывает, что общая смета будет после просмотра
  материалов, и отдельно сохраняет известный ориентир первого этапа;
- `До сдачи / защиты` делает то же для полного маршрута.

Неизвестная общая цена не подменяется выдуманной суммой. Выбор нужен только для
сметы, не запускает работу и остаётся изменяемым до отправки заявки. Обновляется
только существующая строка цены и пояснение; wizard не перерисовывается.

## Preflight и immutable payload

До mutation production был точным `release158-fa2b317`: `current=dist`,
`previous=release157-c891d24`, service `active`, SQLite `ok`, Nginx syntax
валиден, внешний smoke 14/14. Exact source hashes были привязаны до установки.

Две canonical-сборки совпали. Публичный payload содержит 353 файла /
24,987,483 bytes; его deterministic build digest —
`4473647822c40e1d05563ee51a44828691ef49bbc86173fba793cf318c75a55f`.
После сохранения ровно трёх server-owned путей (`.indexnow-key`, SEO notebook,
`sw.js`) immutable tree содержит 356 файлов / 24,997,654 bytes; streamed
SHA-256 manifest digest —
`842dd247a278976e409b8c77ae8e697f7e6ee294edee643286f71d2dbf0a4b53`.
Все 353 public hashes были проверены до activation; owner/mode drift равен 0.

Dry-run относительно release158 показал ровно три изменённых public-файла:

- `configurator.html`:
  `acf6527b8867faab835d34ee6a4754c6a2c6fdcbfc922c7720ae128f1f884341`;
- `assets/css/configurator-journey.css`:
  `97bd43a747eee983f4b281d51bd14f668db1bd4737d71b34f879051e310ffde6`;
- `assets/js/analytics-v2.js`:
  `a97668e6f78775bad7210045dc781bd0e361e733c6f0d5bb91222d583ecbba11`.

Repository, immutable tree and final HTTP bytes совпали для всех трёх.

## Backend-first contract

До переключения static Analytics v2 contract был обновлён с exact hash
`68c9e43d…c0fe2` до 2.3.0 / `e05bfd4b…21f99`. Module, webapp, DB seam,
security headers и incident-hardened Nginx остались byte-identical:

- `webapp.py`: `6c30cf2f…55c05`;
- `db.py`: `7611e08b…dad9`;
- `analytics_v2.py`: `69a9d642…991ea`;
- Nginx site: `e66545c3…9003d`;
- headers: `ebe20503…d77d`.

Первая installer-команда была fail-closed до backup и mutation из-за опечатки
в переданном ожидаемом Nginx hash. Повтор использовал точный preflight hash,
создал root-only backup, не изменил Nginx и перезапустил сервис до green health.
После выполненного rollback финальный forward install создал актуальный rollback
backup:
`/root/salon_bot/backups/analytics-v2-20260822T221327303991Z`.

## Live browser и контрактный smoke

Real Chromium прошёл production-сценарий `Замечания руководителя → ВКР → полный
маршрут`. После выбора строка стала `Смета до сдачи / защиты — после просмотра
материалов`, а ниже остался `Первый этап — от 2 500 ₽`. Матрица
360/390/1024/1440 в light/dark дала root overflow=0 и console errors/warnings=0.
После forward restore отдельный reload на 390 и 1440 повторил тот же результат.
Скриншоты сохранены в ignored
`output/playwright/release159-quote-scope/`.

Один анонимный synthetic visitor без контакта, заказа, текста или файла доказал:

```text
accepted=2: quote_scope_seen(first), quote_scope_continue(full)
rejected=1: variant вне allowlist
duplicate=2: точный replay
revoke: visitors=1, sessions=1, events=2
final residue: visitors=0, sessions=0, events=0, revocations=0
SQLite integrity_check=ok
```

Первые harness-попытки остановились до event ingest: ожидание версии было
сокращено до `2.3`, затем visitor ID не соответствовал exact regex. Два
отклонённых grant увеличили только технический `invalid`; ровно эта дельта была
вычтена, после чего валидный сценарий прошёл. Финальный forward state повторил
весь synthetic цикл и снова оставил zero residue.

## Regression, rollback и final state

До выпуска были зелёными site 580/580, backend 31/31, Brain 39/39, strict Brain
`records=76 links=138 manifests=29`, JS/Python syntax, build и diff check.
`E-1019` ранее доказал byte-identical baseline/current screenshots для
инструментирования. Release review GLM дал GO; Kimi вернул 503, а Sonnet/Opus/
Fable были недоступны из-за истёкшего OAuth. Недоступные endpoints не
представлены как review-доказательство и не заменяют исполняемые проверки.

External и VPS read-only smoke дали 14/14 после activation. Затем был выполнен
полный rollback: оба static pointer вернулись на release158, contract — на
2.2.0; service/Nginx/SQLite и оба smoke снова были green 14/14. Backend-first
forward restore вернул contract 2.3.0, затем `current`/`dist` на release159 и
`previous` на release158; финальные external/VPS smoke снова 14/14.

Final check `2026-08-22T22:16:41Z`: service `active`, SQLite `ok`, Nginx valid,
contract 2.3.0, unauthenticated Analytics overview 403, immutable file/hash/mode
checks green.

## Privacy и граница вывода

Production contacts, orders, files, messages, raw analytics rows, IP/UA,
query/referrer и OAuth material не читались. Просматривались только точные
synthetic IDs, безопасные event names/variants и системные health/hash states;
все synthetic raw rows и tombstone удалены.

Публикация доказывает корректность механики и измерения, но не доказывает рост
конверсии. Uplift можно оценивать только после достаточного органического
consented sample с заранее выбранным окном и guardrails.

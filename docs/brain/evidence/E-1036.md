# E-1036 — REL-0172 production publication proof

- Date: 25 August 2026, Europe/Moscow.
- source_type: exact candidate regression, independent economics/architecture/
  UX review, deterministic static manifest, immutable production readback,
  production browser inspection, two-vantage GET-only smoke and executed
  static rollback-forward drill.
- Canonical product source:
  `df007e40399f47a26bd3903b4702fbcc74755ab4`.
- Implementation result: `925309b2a5380457a336c892ef89fac564898239`.
- Product workstream: `WS-9bec44129f3e4c60a975399edea4e96c`;
  publication workstream: `WS-539a8acc8cf64725b225a5b4b98c8eb5`.
- Outcome: `OUT-006`; gate: `G10`.
- Final static artifact: `release172-df007e4`; initial activation:
  `2026-08-25T15:20:03+03:00`; final forward activation:
  `2026-08-25T15:24:02+03:00`.
- Verdict: **GO** for the bounded smart-rescue release, P0=0/P1=0. This is not
  evidence that separate SEO, legal, pricing or historical UX debt is complete.

## Released outcome and economic boundary

The unfinished configurator exit has four finite reasons. `Цена выше ожиданий`
alone requests the already released qualified retention offer: 10% from a
5,000 RUB order, capped at 2,500 RUB and valid for 72 hours. `Не хватает
материалов`, `Не понимаю состав` and `Нужно согласовать срок` route the visitor
back to the relevant existing continuation without a promo request. The price
branch therefore spends acquisition margin only after an explicit price
objection; the other three branches solve the stated obstacle without an
automatic discount.

Eligibility, server recheck, best-of rules and campaign limits remain the
REL-0169 contract. Returning footprints suppress the rescue completely. The
owner-only link `/configurator.html?offer_preview=retention` is labelled,
presentation-only and non-redeemable: its client-looking action returns to the
reason list and causes no claim, persistent promo marker, navigation or
statistics mutation. Browser Back and an edge swipe close the rescue layer
before the underlying wizard changes step. `pagehide` records only a bounded
local category and performs no network operation.

The supervisor-remarks dossier accepts 40–800 characters, warns against
personal data and transfers the text through a ten-minute same-tab
`sessionStorage` envelope. Only the route code appears in the URL. The
configurator validates and consumes the envelope, then treats the text as an
ordinary local draft comment. When a compatible saved draft exists, the user
explicitly keeps it or starts the new remarks route; keeping the draft neither
overwrites the old comment nor reports a false import. Legacy
`salon_editor_brief` and `salon_prefill_comment` values are scrubbed on the
dossier, and inaccessible/throwing storage fails closed.

No backend, bot, database, API contract, price, tariff, deposit, Salon+,
referral or Kladovaya artifact changed. `deposit.html` remained byte-identical
at `161876c7ab1ac5a19cd9a3f3f1b88db9129c3909184418295d533c87631a83b8`;
deposit issuance remained open throughout REL-0172.

## Independent review and pre-publication proof

- Economics final: GO, P0=0/P1=0. No new money path or network grant exists;
  only the explicit price reason can request the existing bounded offer.
- Architecture final: GO, P0=0/P1=0/P2=0. History ownership, storage lifetime,
  legacy cleanup and preview non-mutation match the declared contract.
- UX final: GO, P0=0/P1=0. Reduced-motion transitions and hover transform are
  disabled. One accepted P2 remains: a legacy private remark already stored in
  another open page is removed when the dossier next opens; active code never
  reads or sends that value, and broader page-wide cleanup was rejected because
  owner preview must remain zero-storage.
- Public Node suite passed 621/621. Backend passed 99/99 with one expected
  local skip for production-only `aiosqlite`. Brain passed 39/39. Focused
  rescue/remarks tests passed 15/15.
- Ordinary Chromium/WebKit mobile proof passed 12/12 at 320/390/430. The
  targeted matrix passed 24/24 at the same widths: contact to rescue to Browser
  Back, both remarks draft-conflict branches, seeded legacy keys and an owner
  action with no POST, storage or application-state change.
- Two clean builds were byte-identical: 358 public files, 26,234,987 bytes,
  relative path-and-content manifest
  `63f2f9ed657e9a128d6c23295345ad5be5f93901be9772a8a1129d95895a00cf`.

Exact changed public hashes were:

- `assets/js/promo-campaign.js`
  `9e46715515aec244e6352adfee1a111cff7510d05149ceadc56b15746ae8687b`;
- `assets/css/promo-campaign.css`
  `a144f8790e98430b311df8e770b5f0f8d179baf453b81083ac547eec6b6fbead`;
- `configurator.html`
  `b519433216225ad98e59efed645ed64e048535b12ba102b3ee3a3537c09609f4`;
- `dosie-nauchruka.html`
  `f608bc81fd38026a7acaaaa2175751655b4d7ec98a84d5eef7a2092d6b8e9063`;
- `assets/js/remarks-handoff.js`
  `de1151dd9830bbf7093ad16e28a7d786f5b8c263ad7664d124cdab740cdd3a61`;
- `index.html`
  `9357cea0f3253b6283c7a275aedeff8eea0bb2e4ceb2ca4533abdfc9524522a7`.

## Static publication and exact remote state

The inactive production tree at
`/var/www/academic_saloon/releases/release172-df007e4` contains 361 files: the
exact 358-file public build plus unchanged `.indexnow-key`, `sw.js` and
`analysis/growth-baseline-2026-07-24.ipynb`. The public subset manifest matches
`63f2f9ed...00cf`; retained hashes match their REL-0171 sources. Owner, file
mode, directory mode, path, symlink and content drift are all zero.

The first local openrsync invocation rejected the GNU-style chmod syntax, and a
second remote invocation rejected a duplicate chown option. Both failed before
pointer activation. A plain checksum transfer followed by exact chown on the
six non-www paths completed, and full manifest/owner/mode validation passed.
Checksum-equal hardlinks received new mtimes, but old release contents, owners,
modes and hashes remained exact. No failed attempt changed `current`, `dist` or
`previous`.

Initial activation at `15:20:03` set `current=dist=release172-df007e4` and
`previous=release171-ffa2421`. The release backup is
`/root/site-backups/release172-smart-rescue-20260825T121703Z`.

## Executed rollback and forward restore

At `15:23:31` the real rollback set `current` and `dist` to
`release171-ffa2421` and restored its exact public hashes, including old index
`6a98c0e2...0109`, configurator `1ac836a7...04a9`, dossier
`b3144526...ccaa`, promo JavaScript `0f3f00dd...cb13d` and promo CSS
`d30cf09e...14644`. Deposit remained byte-identical. External and VPS smoke
each passed 14/14.

At `15:24:02` forward restore returned `current` and `dist` to
`release172-df007e4` and `previous` to `release171-ffa2421`. All six exact new
HTTP hashes returned and external plus VPS smoke each passed 14/14 again.

## Production browser and service proof

Three isolated 390x844 production inspections used the exact live static
files; only the eligibility GET response was controlled to exercise otherwise
unreachable safe states. No response that creates or claims value was mocked.

1. Labelled owner preview rendered at full viewport with exact client CTA
   `Применить 10% и продолжить`. The action returned to the reason list. The URL
   and local storage stayed unchanged; session storage retained only the
   existing `salon_t0`. Requests were GET `/api/auth/session` and GET
   `/api/promo/eligibility`; console errors were zero.
2. A returning footprint plus an eligible response produced no welcome,
   retention, return bar, image or dialog. Requests were GET-only and console
   errors were zero.
3. The remarks route scrubbed seeded legacy keys, moved the exact test remarks
   into the ordinary local draft and textarea, consumed the session envelope,
   cleaned the URL to `/configurator.html` and displayed a truthful status.
   Requests were GET-only and no submit occurred.

No production form, order, contact, file, payment, deposit, bonus, message,
promo claim or retention grant was created.

Final `current` and `dist` are `release172-df007e4`; `previous` is
`release171-ffa2421`. `salon-bot-v2.service` remained active as PID `557663`
with `NRestarts=0` and its original `13:06:18` start time. Nginx configuration
is valid. SQLite remains WAL with `quick_check=ok`; the final health response
was `ok` with 35 active orders. The post-release journal has zero actual
`database is locked`, traceback, ERROR or CRITICAL matches. Broad word matching
also sees ordinary INFO scheduler lines, so those are not reported as errors.

REL-0172 proves deterministic publication, bounded retention routing, privacy
and rollback. It does not prove conversion, revenue, contribution margin or
profit uplift; those require an authoritative consented production sample and
fulfilment-cost data.

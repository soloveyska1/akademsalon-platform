# E-1035 — REL-0171 production publication proof

- Date: 25 August 2026, Europe/Moscow.
- source_type: exact combined-candidate regression, hash-pinned backend
  installer, immutable static manifest, production service/SQLite readback,
  two-vantage GET-only smoke, production Chromium and executed backend plus
  static rollback-forward drills.
- Canonical product source:
  `ffa2421ce9b3aed62b3c508a210fa50a2bb1e438`.
- Entry implementation: `1ee9533df1d677c34b44c95bf8e13ec9a7cda5e3`;
  economic implementation: `4acd0e623f5b2a5e2efd6926f799b71032234c4d`.
- Product workstreams: `WS-2ecb95c1742441bc82768b6c38edcb95` and
  `WS-e92c109191de49b9b1f13b0f1ba1d485`; publication workstream:
  `WS-69dc4870a4f44aafa9e2c93fc7c5425a`.
- Outcomes: `OUT-004`, `OUT-008`; gate: `G10`.
- Final static artifact: `release171-ffa2421`; final forward activation:
  `2026-08-25T13:12:44+03:00`.
- Verdict: **GO** for the bounded first-entry and economic-safety release,
  P0=0/P1=0. This is not evidence that separate legal, SEO or historical UX
  debt is complete.

## Released outcome

The already reviewed first-entry candidate and the later economic-safety
candidate were merged from their exact result SHAs without a product conflict.
The welcome artwork is a 33,652-byte versioned WebP with the existing PNG as an
executable fallback. Returning footprints still suppress the welcome sheet,
and the owner path remains labelled and presentation-only. Application entry
now describes the real three-step route and conditional response window.

Deposits were not paused or removed. The public page keeps the 8/10/12/15
percent ladder and the live wallet CTA. A top-up creates refundable principal;
benefit is earned only from principal actually used and not returned. Ordinary
cashback and deposit value are best-of, not additive; only positive deposit
uplift waits until acceptance plus 14 days. Partial refunds are cumulative and
return exact unused principal. A qualifying first referral settles exactly 200
points once. Eleven persistent SQLite guards reject legacy or inconsistent
money writes, including late awards and spending after a complete-refund
tombstone.

The static content diff from release169 is nine files: first-entry home,
configurator and application copy/artwork plus deposit/refund/economic copy and
their two JavaScript runtimes. No CSS, cabinet page, tariff, price, submit
schema, Analytics contract or Kladovaya artifact changed in REL-0171.

## Combined pre-publication proof

- Exact combined tree: public Node 612/612; backend 99/99 with one expected
  local skip for production-only `aiosqlite`; Brain 39/39; strict validation,
  static build and diff check passed.
- Two consecutive builds were byte-identical: 357 public files, 26,210,824
  bytes, relative path-and-content manifest
  `4a3c2f2245be42ce14213cf5dce5cb10859bd80b60dd04b995dc020d1446b053`.
- The entry candidate's architecture/economics/UX reviews and the economic
  candidate's architecture/economics/UX reviews each ended GO with P0=0/P1=0.
  The economic review loop converted every partial-refund, late-award,
  restoration and cancellation race into an executable production-shape test.
- The preflight production readback found the exact pinned REL-0170 sources,
  no V2 runtime or V2 settings, active service with `NRestarts=0`, valid Nginx,
  WAL and `quick_check=ok`. Operator-network GET smoke passed 14/14 before the
  first mutation.

## Backend publication and rollback-forward

An online SQLite safety copy was created at
`/root/salon_bot/backups/release171-pre-20260825T100556Z/salon.db` and returned
`quick_check=ok`. It was not restored. The bot was stopped only for the bounded
source/database migration: backend start `13:06:17`, ready `13:06:21` MSK.
The exact source pre-images were copied to
`/root/salon_bot/backups/economic-v2-20260825T100618364976Z`.

Apply and final `--check` returned source state `after`, eleven exact triggers,
no missing/tampered/unexpected trigger and
`bad_lots=bad_allocations=bad_rewards=bad_refunds=0`. One legacy pending
deposit was preserved for reconciliation and one legacy qualifying referral
became a review obligation; neither was silently granted or discarded. Final
settings are:

- `economic_deposit_contract_v2=earned-v2:on`;
- `economic_deposit_issuance_v2=earned-v2:open`;
- `economic_referral_contract_v2=fixed-200:on`.

The executed backend rollback at `13:07:17` closed only new top-ups and referral
grants (`earned-v2:closed`, `fixed-200:review`) while existing principal,
paid holds and earned rewards stayed serviceable. Exact forward apply at
`13:07:18` restored the three final settings and source hashes. SQLite was
never replaced. Final service PID `557663` had `NRestarts=0`; after 442 seconds
the release journal still contained zero `database is locked`, traceback,
scheduler failure, ERROR or CRITICAL matches. WAL and `quick_check=ok` remained
unchanged, and temporary installer files were zero.

## Static publication and rollback-forward

The inactive tree contained the exact 357-file build plus the three unchanged
server-owned files `.indexnow-key`, `sw.js` and the growth-baseline notebook:
360 files / 26,220,995 bytes. Owner/mode drift was zero. The retained files
kept hashes `f3930289...af8`, `f500c4f3...648` and `0929da3e...039`.

Initial activation at `13:07:52` moved `current` and `dist` to
`release171-ffa2421` and `previous` to `release169-51f3556`. Live HTTP matched
the release hashes, including:

- `index.html` `6a98c0e2...109`;
- `deposit.html` `161876c7...3b8`;
- `assets/js/promo-campaign.js` `0f3f00dd...13d`;
- `promo-salon-welcome.webp` `ec6ed0c1...f69`.

At `13:12:08` the real static rollback restored `current`/`dist` to release169
and `previous` to release168. The exact old index, deposit and promo hashes
returned; VPS and operator smoke each passed 14/14. At `13:12:44` forward
restore returned release171/release169, all four new hashes and 14/14 from both
vantage points. Nginx stayed valid, service stayed active and final temporary
pointer residue was zero.

## Production browser proof

A new isolated production Chromium session displayed the welcome sheet. Its
current image was the versioned WebP, complete at intrinsic 960x720. At
390x844 the sheet rectangle was exactly `0,0..390,844`; at 1440x900 it was
`200,14..1240,886`. Root and body scroll widths equalled the viewport, focus
started on the labelled close button, and the console had zero errors/warnings.

The live deposit page at both viewports exposed exact rates 8/10/12/15, no
pause/closed copy and `Перейти в кошелёк -> dashboard.html#wallet`. The mobile
CTA was 352x48 px and root width remained 390 with no horizontal overflow. No
form, order, contact, file, payment, deposit, bonus, message, promo claim or
retention grant was created for browser proof.

## Final boundary and rollback

Final production truth is `current=dist=release171-ffa2421`,
`previous=release169-51f3556`, active V2 backend and open deposit issuance.
The release backup is
`/root/site-backups/release171-september-20260825T100556Z`.

Immediate backend rollback is the pinned `--disable` state; keep the V2 service
adapter until every lot, hold and claim is settled, and never restore SQLite as
a routine rollback. Static rollback is the exact immutable release169 pointer.
This release proves publication and safety mechanics. It does not prove field
Core Web Vitals, conversion, revenue, contribution margin or profit uplift.

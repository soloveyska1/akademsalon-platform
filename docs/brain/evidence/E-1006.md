# E-1006 — OUT-003 shared shell state contract

- Date: 2026-08-03, Europe/Moscow
- source_type: deterministic-local
- Canonical base: `65adc47cf31ffaa3ba9a797204d938ef866f0e14`
- Failing-first commit: `c8fd314` (2/7 pass, 5/7 fail)
- Implementation commit: `7e6f33a6088888ccf49dbbd81cb2a8f68c9cecc2`
- Workstream: `WS-c4d669dfa50e4493a2fbef81f3481ede`
- Mutations: no production submit, API write, OAuth, deploy, client-data access or
  deletion. The temporary localhost server and Browser tabs were stopped.

## Audited topology and baseline

The inventory resolves all 92 root HTML documents into one explicit delivery
and shell family. Ninety managed documents share the changed shell assets;
`index.html` receives the same source through reproducible home bundles, while
`admin-covers.html` and `oplaceno.html` remain standalone exceptions.

No P0 was reproduced. Five independent red contract groups exposed P1 defects
that the earlier green source suites did not cover:

- saved dark theme was lost on five legal/consent routes without an early
  `salon_theme` bootstrap;
- the dark mobile footer primary and unread/auth badges used normal-size light
  text on a wax pair below 4.5:1;
- `admin.html` loaded both public and admin Cmd/Ctrl+K owners;
- consent preferences declared a modal without making background siblings
  inert;
- legal copy promised persistent “Настройки данных”, but the shared footer had
  no opener and eight ordinary routes omitted the consent runtime.

Three read-only agents independently reproduced theme, consent, shortcut and
contrast findings. Root remained the only write-owner. No approved screen was
redesigned.

## Implemented contract

- Every public route now has early saved-theme parity and a persistent consent
  settings path. The eight missing ordinary routes load the existing extras
  presentation/runtime.
- Consent preferences record prior sibling inertness, isolate the background
  while open, restore it on close and return focus to the exact opener.
- `admin.html` no longer loads the public dialog/shortcut owner.
- Mobile footer primary, desktop/mobile unread badges and the normal-text gold
  action use the existing accessible `--wax-cta` token.
- Direct consumers and the generated home bundles use one cache key,
  `20260803out003shell1`; the build remains source-derived and byte-stable.
- `tests/shared-shell-contract.test.js` freezes the route-family, theme,
  consent, admin, contrast, auth/footer/overlay and cache-parity invariants.

## Deterministic gates

- Red baseline at `c8fd314`: 7 contract tests, 2 pass and 5 fail.
- Final focused contract/cache/regression family: 78/78 pass.
- Full repository suite: 472/472 pass, 0 fail, 0 skipped.
- `node --check assets/js/app.js` and `assets/js/extras.js`: pass.
- Re-running `node scripts/build-home-release.mjs` preserved both bundle hashes:
  CSS `8e22abfe7d638a94d81583b75c023bee85f626f1`, JS
  `24c0f7bed79e0cac6ee784270761bc110c294f8d`.
- Brain unit suite: 39/39 pass; strict corpus validation:
  `VALID records=46 links=37 manifests=6` before this evidence projection.
- `git diff --check`: pass.
- Conflict scan: hard=0, warnings=1, info=3. The integration owner explicitly
  accepted only the unmanaged dormant-ref overlap; no active dirty overlap was
  present. A fresh fetch and scan remain mandatory immediately before merge.

## Browser proof

Evidence environment: Codex in-app Browser Plugin against
`http://127.0.0.1:8765`, local anonymous data, persisted dark theme, no external
or production requests. The plugin did not expose its engine build string; that
is an explicit G9 metadata limit rather than an inferred browser version.

Exact bundled-home/standard spine:

| Routes | Viewport | Expected shell | Actual |
|---|---:|---|---|
| `index.html`, `services.html` | 360×800, 390×844, 768×900 | mobile appbar, footer/settings, no overflow | pass; dark preserved, overflow 0 |
| `index.html`, `services.html` | 1024×800, 1440×900 | desktop header, footer/settings, no overflow | pass; dark preserved, overflow 0 |

The 390×844 mobile footer action measured 44 px on both routes. Computed contrast
was 5.827:1 on `services.html` (`#fff8ef` on `#A4432D`) and 4.773:1 on bundled
home (`#fff8ef` on `#C83E27`). Header/dock badge contrast was 6.082:1.

Route exceptions were checked at exact 390×844 and 1440×900:

- legal/404 retained the standard shell and settings path;
- account retained its account header/appbar while intentionally hiding the
  public footer;
- configurator retained its task shell;
- admin exposed neither public header/footer nor public dialogs;
- all ten route/viewport observations had overflow 0 and no open dialog.

Navigating from a dark standard route to `academic-integrity.html` preserved
`data-theme=dark`, dark theme-color and pressed theme state. Menu and search
each opened one native dialog, focused inside, closed on Escape and returned
focus to the exact trigger. Consent preferences opened from the unique footer
link, set `inert` on all 14 background body children, removed all inert residue
on Escape and returned focus to that link. Final `services.html` console
error/warning log was empty.

Reduced-motion and anonymous/guest/authenticated invariants are executable
source contracts; the browser run did not emulate reduced motion or perform
live OAuth. This outcome therefore makes no live-auth or production-delivery
claim.

## Independent council

Worktree doctor and live probe returned Kimi, Sonnet, GLM, Opus and Fable
`READY`; `local_llm: DISABLED`, `background_services: NONE`. Kimi/TokenRouter
and GLM/Z.AI used portable Keychain credentials; Sonnet/Opus/Fable used the
global Claude subscription session. No secrets or `.brain/council` reports are
tracked.

Daily independent review used Kimi+Sonnet+GLM. Kimi and GLM challenged the dark
contrast; Sonnet identified the existing `--wax-cta` precedent and the second
footer rule. One Opus review required exact computed-selector evidence,
source/home-bundle parity and a single cache wave. Those requirements became
the deterministic and browser gates above. Fable remained connectivity-only
because no systemic deadlock existed; model output did not close any hard gate.

## Product conclusion

Main/home, shared header/appbar/footer, local anonymous client entry and the
integrated cabinet slices have no reproduced P0/P1 in the checked contract.
`OUT-001` remains externally gated by missing authoritative backend/bot source
and safe marker/lookup/cleanup; this result does not convert UI success into a
delivery claim.

The next locally provable product slice is `OUT-005`: simplify `services.html`
from a broad catalogue into a situation-led choice that hands one explicit,
truthful context to the configurator while preserving every physical SEO URL,
canonical, schema, sitemap and price contract.

# E-1044 — psychology VIP entry clarity

- Date: 31 August 2026, Europe/Moscow.
- source_type: contract tests, deterministic build, WebKit/Chromium mobile
  smoke, real browser navigation, two independent reviews, exact production
  hashes, public readback and executed static rollback-forward.
- Authoritative implementation:
  `98973234c142abf2aed9e0dba8281566786d0c4a`.
- Integrated remote revision:
  `5365ae6b523324aa7b16af7f83570471ac73f357`.
- Workstream: `WS-c0c824edb7ef4c1783f7350456906290`.
- Outcome: `OUT-006`; release: `REL-0180`.
- Final forward: `2026-08-31T15:19:39+03:00`.

## Bounded result

The fixed 91,000 RUB psychology VIP route now explains the package before it
asks for project details. The first form screen shows the full-project route,
four outcome groups, the fixed price, scope-expansion boundary, and the staged
27,300 / 36,400 / 27,300 payment request. The question that accepts one chip is
now explicitly the strongest change rather than falsely implying that only one
thing changed.

The owner still always has access to the welcome-offer preview, but the
configurator no longer opens it as a blocking modal on arrival. A labelled
compact launcher opens the existing GET-only preview on explicit action. The
VIP action bar is in normal document flow on mobile and removed as an overlay
on desktop.

The psychology landing re-synchronises the checked offer on `pageshow`.
`VIP -> configurator -> Back` therefore preserves the 91,000 RUB CTA, open VIP
scope and `service=pv` destination instead of silently reverting the CTA to
the 29,000-40,500 RUB editing route.

## Protected boundaries

The 91,000 RUB price, included scope, A2 participation, privacy gate, order
payload, deposits, discounts, eligibility rules, best-of selection and 25%
combined-benefit ceiling are unchanged. No order or payment is created before
explicit form submission. The clinical-material de-identification requirement
remains mandatory.

## Reproducible verification

- Public Node suite: 648/648.
- `npm run build`, JavaScript/inline syntax checks, `git diff --check`, Brain
  validation and the declared conflict snapshot pass; only known terminal
  worktree/unmanaged-ref warnings remain.
- WebKit and Chromium mobile smoke at 320, 390 and 430 px: 6/6.
- Real browser desktop readback shows the VIP passport before fields and no
  fixed task bar. Real BFCache navigation returns with `checked=true`, the VIP
  CTA, open scope and an href containing `service=pv`.
- Authenticated production owner readback shows `brief=true`,
  `launcher=true`, `modal=false`, and the task bar hidden on arrival. The
  launcher opens the labelled owner preview and closing it restores the page.
- Two independent final reviews returned GO with P0=0/P1=0.

## Production readback and rollback

Immutable static release `release180-9897323` was copied from release179 and
overlaid with exactly six public files. It has 369 files / 26,308,866 bytes,
zero internal symlinks and a sorted file digest of
`2c96dbb0ab218d3cf2689df789bab7e75c33fd24675b89a6b5fa20804639a524`.

Final production hashes match the authoritative local source:

- `index.html`: `934bc6209905bbfb918fce0008cba40d47bcb00b120beb8bbfacb4f9b0238c8c`;
- `configurator.html`: `fc9ad364f01f2d23742c6629ff7ac47048197da56ce3947fd902dd86d6483bc3`;
- `diplomnaya-po-psihologii.html`: `4d4b3a5c9ed3be5fafc60cf698f98c47abd9866fab0f97b79bdd3f871e3b37ed`;
- `assets/js/promo-campaign.js`: `a51cc79126c52453e268753737f3391950c0732f5e7b97f1f55e58b8e42b5124`;
- `assets/css/promo-campaign.css`: `012a94171eb7c966593f985b5bf6ce8996d427781d860652222cb1141aeef651`;
- `assets/css/psychology-vkr.css`: `16a65b930d4b5035fdca8adde572b35febe560a5ee55815d078f7ef680c4ae5e`.

The release was activated, rolled back to release179, smoke-tested at HTTP 200,
and restored forward to release180. Final `current` and `dist` point to
release180; `previous` points to release179. Public home, psychology landing,
VIP configurator and `/api/health` return HTTP 200. Nginx syntax is valid,
`salon-bot-v2` is active with `NRestarts=0`, SQLite is in WAL mode with
`quick_check=ok`, and the post-release journal has zero
lock/busy/traceback/CRITICAL matches.

Private Sites version 42 also deployed successfully at
`https://akademsalon-desktop-preview.saymoon.chatgpt.site`; deployment
`appgdep_6a957121802081919cb0243a46b6ef11`, saved version
`appgprj_6a66b7b3535c819198c9c962f50dc7b7~appgver_b247c60a60508191b4b65f8f58fa91dd`.

## Remaining boundary

No real customer order was created during verification and no conversion
uplift is claimed. The one-choice primary-change question intentionally remains
bounded; secondary changes belong in the free-text requirements field until
real traffic supports a larger multi-select data-contract change.

(function(){'use strict';const cards=Array.from(document.querySelectorAll('.rv-card'));if(!cards.length)return;const dialog=document.querySelector('.rv-viewer'),canvas=dialog.querySelector('.rv-canvas'),image=canvas.querySelector('img'),more=document.querySelector('.rv-more'),filters=Array.from(document.querySelectorAll('[data-review-filter]'));let current='all',limit=9,index=0,opener=null;const filtered=()=>cards.filter(c=>current==='all'||c.dataset.topic===current);
function render(){const set=filtered();cards.forEach(c=>c.hidden=!set.includes(c)||set.indexOf(c)>=limit);document.querySelector('#rv-count').textContent=Math.min(set.length,limit)+' из '+set.length+' сообщений';more.hidden=set.length<=limit;filters.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.reviewFilter===current)));}
filters.forEach(b=>b.addEventListener('click',()=>{current=b.dataset.reviewFilter;limit=9;render();}));more.addEventListener('click',()=>{const first=filtered()[limit];limit+=9;render();first?.focus({preventScroll:true});});
function show(){const set=filtered(),card=set[index];if(!card)return;canvas.classList.remove('is-zoomed');canvas.scrollTo(0,0);dialog.querySelector('[data-rv-zoom]').textContent='Увеличить ＋';dialog.querySelector('[data-rv-zoom]').setAttribute('aria-pressed','false');image.src=card.href;image.alt=card.querySelector('img').alt;document.querySelector('#rv-title').textContent=image.alt;document.querySelector('#rv-original').href=card.href;document.querySelector('#rv-position').textContent=(index+1)+' / '+set.length;dialog.querySelector('[data-rv-prev]').disabled=index===0;dialog.querySelector('[data-rv-next]').disabled=index===set.length-1;}
cards.forEach(c=>c.addEventListener('click',e=>{if(e.ctrlKey||e.metaKey||e.shiftKey||e.altKey||!dialog.showModal)return;e.preventDefault();opener=c;index=filtered().indexOf(c);show();dialog.showModal();dialog.querySelector('[data-rv-close]').focus();}));dialog.querySelector('[data-rv-close]').addEventListener('click',()=>dialog.close());dialog.addEventListener('close',()=>opener?.focus({preventScroll:true}));dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});function move(n){const next=index+n;if(next<0||next>=filtered().length)return;index=next;show();}dialog.querySelector('[data-rv-prev]').addEventListener('click',()=>move(-1));dialog.querySelector('[data-rv-next]').addEventListener('click',()=>move(1));dialog.addEventListener('keydown',e=>{if(canvas.classList.contains('is-zoomed'))return;if(e.key==='ArrowRight'){e.preventDefault();move(1);}if(e.key==='ArrowLeft'){e.preventDefault();move(-1);}});dialog.querySelector('[data-rv-zoom]').addEventListener('click',e=>{const on=canvas.classList.toggle('is-zoomed');e.currentTarget.textContent=on?'Вписать ↙':'Увеличить ＋';e.currentTarget.setAttribute('aria-pressed',String(on));canvas.scrollTo(0,0);});render();})();

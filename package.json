{
  "name": "akademsalon-site",
  "version": "1.0.0",
  "private": true,
  "description": "Статический сайт и проверки релиза Академического Салона",
  "scripts": {
    "audit": "node scripts/site-audit.mjs",
    "build": "node scripts/build-public.mjs",
    "test": "node --test tests/*.test.js && npm run audit"
  },
  "engines": {
    "node": ">=20"
  }
}

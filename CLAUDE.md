# Академический Салон

Перед работой прочитайте `AGENTS.md` и выполните:

```bash
./bin/brain context --task "краткая цель"
./bin/council --doctor --providers kimi,sonnet,glm,opus,fable --allow-fable
```

Project brain находится в `docs/brain/`. Он является долговечной памятью проекта;
чат и сырые модельные отчёты не заменяют решения, evidence, UX debt и handoff.
Не запускайте локальные LLM, watchers или постоянные фоновые сервисы.

## Это не единственный проект владельца

«Кладовая ГИПСР» (studkladovaya.ru) — **отдельный** проект: код в
`~/Documents/Codex/2026-07-30/https-t-me-kladovaya-gipsr`, стек Next.js, свой сервер,
свой `CLAUDE.md` и своя память. Правила этого файла к ней не относятся, и наоборот.
Задачу по Кладовой делать в её папке, а не здесь — иначе она не увидит ни своей
памяти, ни своих проверок.

--- a/root/salon_watch/watch.py
+++ b/root/salon_watch/watch.py
@@ -5 +5 @@
-диск, сертификат, свежие 5xx/404 в nginx, клиентские JS-ошибки и трейсбеки
+диск, сертификат, свежие 5xx в nginx, клиентские JS-ошибки и трейсбеки
@@ -50 +49,0 @@
-THROTTLE_404 = 60 * 60
@@ -54 +52,0 @@
-SPIKE_404 = 40                # 404 за интервал, дальше — подозрительно
@@ -232,3 +230,8 @@
-def nginx_counts(lines: list[str]) -> tuple[int, int, list[str]]:
-    """→ (n404, n5xx без 503, примеры 5xx)."""
-    n404 = n5 = 0
+def nginx_server_errors(lines: list[str]) -> tuple[int, list[str]]:
+    """→ (n5xx без maintenance 503, примеры).
+
+    Публичные 404 намеренно не являются health-сигналом: автоматические
+    сканеры создают их на исправном статическом сайте. Внутренние ссылки
+    проверяются детерминированными link/sitemap-тестами и smoke.
+    """
+    n5 = 0
@@ -241,3 +244 @@
-        if code == "404":
-            n404 += 1
-        elif code[0] == "5" and code != "503":
+        if code[0] == "5" and code != "503":
@@ -251 +252 @@
-    return n404, n5, samples
+    return n5, samples
@@ -331 +332 @@
-    day = st.setdefault("day", {"404": 0, "5xx": 0, "js": 0, "tb": 0})
+    day = st.setdefault("day", {"5xx": 0, "js": 0, "tb": 0})
@@ -332,0 +334,4 @@
+    day.pop("404", None)
+    thr.pop("404", None)
+    for key in ("5xx", "js", "tb"):
+        day.setdefault(key, 0)
@@ -335,2 +340 @@
-    n404, n5, samples = nginx_counts(lines)
-    day["404"] += n404
+    n5, samples = nginx_server_errors(lines)
@@ -348,5 +351,0 @@
-    if n404 >= SPIKE_404 and now_ts() - thr.get("404", 0) > THROTTLE_404:
-        thr["404"] = now_ts()
-        alerts.append(f"🟡 Всплеск 404: {n404} обращений к несуществующим страницам "
-                      "за пару минут — похоже на сканер, но взгляните в «Визиты».")
-
@@ -389 +388 @@
-       f"За сутки: 404 — {day.get('404', 0)} · серверных ошибок — {day.get('5xx', 0)} · "
+       f"За сутки: серверных ошибок — {day.get('5xx', 0)} · "
@@ -393 +392 @@
-    st["day"] = {"404": 0, "5xx": 0, "js": 0, "tb": 0}
+    st["day"] = {"5xx": 0, "js": 0, "tb": 0}
@@ -413 +412 @@
-           "ошибки, всплески 404, клиентские JS-ошибки и трейсбеки бота.\n"
+           "ошибки, клиентские JS-ошибки и трейсбеки бота.\n"
